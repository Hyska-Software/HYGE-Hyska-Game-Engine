# Goal: R-104 Windows Editor Distribution

Execute the R-104 package plan in `PLAN.md`.

- Keep Rust as the runtime truth owner.
- Preserve source `.py` frontend execution for development.
- Execute packaged `.exe` frontends directly, without Python or PATH.
- Load packaged QML through Qt resources.
- Prove the result with the real R-103 fixture and retained evidence.
- Report `implemented but unproven` if clean-machine evidence cannot be captured.
