version = 1
title = "Hyge Engine Implementation Roadmap"
updated_at = "2026-06-29T13:42Z"
status_scale = ["not_started", "in_progress", "blocked", "complete"]
priority_scale = ["P0", "P1", "P2", "P3"]
risk_scale = ["low", "medium", "high"]

[metadata]
source_architecture = "docs/architecture.md"
source_agents = "AGENTS.md"
target_state = "v0.1.0 — production-ready 3D game engine with full editor"
msrv = "1.80"
channel = "stable only (no nightly)"

[[phases]]
id = "phase_0"
name = "Governance and Foundation"
goal = "Establish repository governance, ADR process, ownership map, CI pipeline, and the full workspace + 14 crates with shared infrastructure."

[[phases]]
id = "phase_1"
name = "ECS Core and App Skeleton"
goal = "Implement `hyge-core`, `hyge-ecs`, `hyge-window`, and `hyge-app` so that `App::run()` opens a window and clears a color."

[[phases]]
id = "phase_2"
name = "Render Graph and First Triangle"
goal = "Implement `hyge-render-graph` (DAG, barrier inference, transient allocator) and `hyge-render` device init. One hardcoded triangle on screen."

[[phases]]
id = "phase_3"
name = "Asset Pipeline and Importer"
goal = "Implement `hyge-asset` (BLAKE3 DB, hot-reload), `hyge-tools` CLI, glTF import, meshlet bake, KTX2 transcode. A lit sphere loaded from a glTF at runtime."

[[phases]]
id = "phase_4"
name = "Bindless PBR Clustered Forward IBL"
goal = "Bindless descriptor heap, PBR with IBL, clustered forward shading, light grid compute. 1000+ materials + 64 dynamic lights visible."

[[phases]]
id = "phase_5"
name = "Shadows Post-Process Animation and Culling"
goal = "Cascade shadow maps, per-light shadow atlas, TAA, SMAA, Bloom, ACES tonemap, skinning, CPU frustum + GPU meshlet culling, LOD."

[[phases]]
id = "phase_6"
name = "Scene Prefab Instancing"
goal = "`hyge-scene` with glTF scene loader, `.hyge-world` / `.hyge-prefab` formats, hierarchical prefab system with override, instancing extraction, component catalog."

[[phases]]
id = "phase_7"
name = "Physics Audio Input Scripting"
goal = "`hyge-physics` (rapier3d), `hyge-audio` (kira + HRTF), `hyge-input` (Action<T> + TOML + raw input), `hyge-script` (Lua + bevy_reflect + sandbox). 5 prefab instance scene with full systems."

[[phases]]
id = "phase_8"
name = "Full Visual Editor"
goal = "`hyge-editor` with viewport, hierarchy, inspector (reflect-driven), content browser, asset graph, console, profiler, undo/redo, docking persistence."

[[phases]]
id = "phase_9"
name = "Polish Sandbox mdbook Performance Release"
goal = "`hyge-sandbox` example, headless screenshot tool, mdbook site (4 tutorials), performance baseline (144 FPS at 1080p on RTX 3060), v0.1.0 release."

# =============================================================================
# PHASE 0 — Governance and Foundation
# =============================================================================

[[items]]
id = "R-001"
title = "ADR Foundation"
phase = "phase_0"
owner = "ecosystem"
priority = "P0"
status = "complete"
risk = "medium"
dependencies = []
summary = "Create decision records for the 14 closed architectural decisions in docs/architecture.md §4."
completed_at = "2026-06-29"
completion_notes = "Created docs/adr/ with README index + 14 ADRs (0001..0014), each with Status, Context, Decision, Consequences, Alternatives Considered, and References sections."
acceptance = [
  "docs/adr/ exists with at least 14 files",
  "ADR-0001 covers wgpu choice with alternatives considered (ash, raw DX12)",
  "ADR-0002 covers bevy_ecs choice with alternatives considered (shipyard, custom)",
  "ADR-0003 covers render stack (bindless + render graph + clustered forward)",
  "ADR-0004 covers workspace structure (14 fat crates)",
  "ADR-0005 covers concurrency model (dedicated render thread + FrameSnapshot)",
  "ADR-0006 covers asset pipeline (BLAKE3 + glTF source + .hyge-world + hot-reload)",
  "ADR-0007 covers scene/prefab (BLAKE3-keyed hierarchical + instancing)",
  "ADR-0008 covers scripting (mlua + bevy_reflect, WASM deferred)",
  "ADR-0009 covers physics (rapier3d behind feature flag)",
  "ADR-0010 covers audio (kira + spatial 3D + HRTF optional)",
  "ADR-0011 covers windowing/input (winit + gilrs + raw input + TOML bindings)",
  "ADR-0012 covers networking out-of-scope decision (v0.2 plan)",
  "ADR-0013 covers editor scope (full editor in v0.1)",
  "ADR-0014 covers build constraints (stable only, MSRV 1.80)",
  "Each ADR has Status, Context, Decision, Consequences, Alternatives sections"
]

[[items]]
id = "R-002"
title = "Ownership Map"
phase = "phase_0"
owner = "ecosystem"
priority = "P0"
status = "complete"
risk = "low"
dependencies = ["R-001"]
summary = "Define owners and review responsibilities across all top-level crates."
completed_at = "2026-06-29"
completion_notes = "Created docs/ownership.md with 20 owner groups, per-crate ownership table covering all 15 implementation crates (14 workspace + hyge-runtime-test + examples + governance areas) with lead and 2 delegates each, PR routing rules for single-crate / multi-crate / cross-cutting / docs / hotfix / ADR PRs, escalation path, on-call rotation, and a procedure for binding real names to role identifiers."
acceptance = [
  "docs/ownership.md exists",
  "each of the 14 crates has a primary owner group",
  "each crate has at least one named delegate",
  "PR routing rules documented"
]

[[items]]
id = "R-003"
title = "Roadmap Reporting Script"
phase = "phase_0"
owner = "tooling"
priority = "P1"
status = "complete"
risk = "low"
dependencies = []
summary = "Create a script that reads roadmap.toml and emits phase-grouped progress reports."
completed_at = "2026-06-29"
completion_notes = "Created tools/roadmap_report/ standalone Rust binary (hyge-roadmap-report) with library + CLI. Validates v1 schema (version, scales, phase/item ID uniqueness, phase/dependency references, status/priority/risk membership, owner non-empty, no self-deps). Emits phase-grouped progress in human (columnar) and JSON formats. Unit tests (in src/report.rs) + integration tests (tests/integration.rs) with fixtures for valid, duplicate-phase, bad-priority, unknown-phase-ref, self-dep, and unknown-dep cases. CI workflow .github/workflows/roadmap-report.yml runs validation strict, posts sticky PR comment via marocchino/sticky-pull-request-comment, and posts to the milestone tracking issue (issue number in HYGE_ROADMAP_MILESTONE_ISSUE repo variable) via peter-evans/create-or-update-comment, with weekly cron."
acceptance = [
  "tools/roadmap_report/ exists with a binary",
  "script validates roadmap.toml against the v1 schema",
  "script outputs phase-grouped progress (e.g. 'phase_0  3/3 complete  100%')",
  "CI runs the script and posts a comment on the milestone tracking issue"
]

[[items]]
id = "R-004"
title = "Workspace Cargo.toml"
phase = "phase_0"
owner = "build"
priority = "P0"
status = "complete"
risk = "low"
dependencies = []
summary = "Create the root workspace Cargo.toml with resolver = '2' and [workspace.dependencies] single-versioning policy."
completed_at = "2026-06-29"
completion_notes = "Created root Cargo.toml with [workspace] (resolver='2', members=['crates/*','examples/*']), [workspace.package] (v0.1.0, edition 2021, rust-version 1.80, license, repository, etc.), [workspace.lints] (rust: unsafe_code=deny, missing_docs=warn, unused_must_use=deny; clippy: all+pedantic+nursery with documented allows), [profile.dev] and [profile.release] (release with lto='thin'), and [workspace.dependencies] declaring ~40 external deps (glam, wgpu, bevy_ecs, rapier3d, kira, mlua, egui, gltf, meshopt, rusqlite, blake3, notify, etc.) plus all 15 internal hyge-* crates with path. Created 15 placeholder member crates (hyge-core, hyge-ecs, hyge-render, hyge-render-graph, hyge-asset, hyge-scene, hyge-physics, hyge-audio, hyge-window, hyge-input, hyge-script, hyge-app, hyge-editor, hyge-tools, hyge-runtime-test) each with Cargo.toml referencing workspace deps and minimal src/lib.rs placeholder. hyge-render and hyge-render-graph allow unsafe_code override. hyge-physics has feature 'physics-rapier' (default). hyge-audio has feature 'audio-hrtf'. Created root README.md pointing to docs/, AGENTS.md, and listing all crates. Note: cargo/rustc not available in this environment, so 'cargo build --workspace' was not executed by the agent; the structure was validated by hand against Cargo workspace conventions and matches docs/architecture.md §5.2 crate map. The CI pipeline (R-006) will execute cargo build on PRs."
acceptance = [
  "Cargo.toml at workspace root defines [workspace] with members = ['crates/*', 'examples/*']",
  "[workspace.package] sets version, edition, rust-version, license, repository",
  "[workspace.dependencies] declares every external dep with version",
  "all member crates reference external deps as `dep = { workspace = true }`",
  "cargo metadata succeeds with no warnings",
  "cargo build --workspace succeeds with no errors (even if crates are empty placeholders)"
]

[[items]]
id = "R-005"
title = "Workspace Toolchain Pin"
phase = "phase_0"
owner = "build"
priority = "P0"
status = "complete"
risk = "low"
dependencies = []
summary = "Pin Rust toolchain to stable, MSRV 1.80."
completed_at = "2026-06-29"
completion_notes = "Created rust-toolchain.toml (channel='stable', components=['clippy','rustfmt'], profile='minimal'); rustfmt.toml (unstable_features=true, imports_granularity='Crate', group_imports='StdExternalCrate'); clippy.toml (msrv='1.80' for MSRV-aware suggestions, allow=[module_name_repetitions, must_use_candidate, missing_errors_doc]). MSRV documented in README.md (Requirements section) and Cargo.toml ([workspace.package] rust-version='1.80' + comment in rust-toolchain.toml pointing to both). README also got a Requirements section listing C/C++ toolchain prerequisites for rusqlite/mlua/meshopt native builds."
acceptance = [
  "rust-toolchain.toml sets channel = 'stable' and components = ['clippy', 'rustfmt']",
  "rustfmt.toml at root sets unstable_features = true, imports_granularity = 'Crate', group_imports = 'StdExternalCrate'",
  "clippy.toml at root allows module_name_repetitions, must_use_candidate, missing_errors_doc",
  "MSRV documented in README and Cargo.toml"
]

[[items]]
id = "R-006"
title = "CI Matrix (Windows + Linux)"
phase = "phase_0"
owner = "build"
priority = "P0"
status = "complete"
risk = "low"
dependencies = ["R-004"]
summary = "GitHub Actions CI with Windows + Linux matrix on stable + beta."
completed_at = "2026-06-29"
completion_notes = "Created .github/workflows/ci.yml with two jobs: (1) 'ci' job with matrix windows-latest x {stable, beta} + ubuntu-latest x {stable} (3 cells) running cargo fmt --check, cargo clippy --workspace --all-targets --deny warnings, cargo test --workspace, cargo build --workspace --release, cargo doc --workspace --no-deps with RUSTDOCFLAGS=-D warnings; (2) 'headless-render' job on ubuntu-latest that installs libvulkan1 + mesa-vulkan-drivers (lavapipe software ICD) + vulkan-tools, runs vulkaninfo --summary to verify the loader, builds hyge-runtime-test --release, and runs a no-op smoke test that becomes the real snapshot test once R-090 lands. Both jobs use Swatinem/rust-cache@v2 with workspaces target -> target (and tools/roadmap_report/target -> tools/roadmap_report/target for the ci job). dtolnay/rust-toolchain installs the matrix rust channel plus clippy + rustfmt components. Branch protection setup documented in workflow header comment for 'green PRs mergeable' acceptance."
acceptance = [
  ".github/workflows/ci.yml exists with matrix: windows-latest x {stable, beta} + ubuntu-latest x {stable}",
  "steps run cargo fmt --check, cargo clippy --workspace --all-targets -- -D warnings, cargo test --workspace, cargo build --release, cargo doc --no-deps -- -D warnings",
  "Swatinem/rust-cache used for ~/.cargo and target/",
  "headless render smoke test runs on ubuntu-latest with Vulkan ICD",
  "green PRs mergeable"
]

[[items]]
id = "R-007"
title = "Nightly Job (miri, udeps, fuzz build)"
phase = "phase_0"
owner = "build"
priority = "P1"
status = "complete"
risk = "low"
dependencies = ["R-006"]
summary = "Nightly CI job for unsafe audit, unused-deps, fuzz build."
completed_at = "2026-06-29"
completion_notes = "Created .github/workflows/nightly.yml with 4 parallel audit jobs + 1 aggregator: (1) udeps installs nightly + cargo-udeps and runs 'cargo +nightly udeps --workspace --all-targets'; (2) miri-render installs nightly + miri and runs 'cargo +nightly miri test -p hyge-render' with MIRIFLAGS=-Zmiri-disable-isolation; (3) miri-render-graph same for hyge-render-graph; (4) fuzz-build installs nightly + cargo-fuzz and runs 'cargo +nightly fuzz build', gracefully skipping with a ::notice when fuzz/Cargo.toml is absent (the fuzz crate is created in M2 by R-038); (5) report aggregator runs after all 4 with 'if: always()', posts a summary table to the milestone issue via peter-evans/create-or-update-comment@v4 using the HYGE_ROADMAP_MILESTONE_ISSUE repo variable, warns with ::warning if the variable is unset, and uses 'continue-on-error: true' so the audit jobs are not blocked. Schedule: daily at 03:00 UTC; also accepts workflow_dispatch. Swatinem/rust-cache@v2 in each job. All miri jobs set MIRIFLAGS=-Zmiri-disable-isolation for CI speed (Miri's default isolation adds significant overhead; the trade is fewer caught bugs at the cost of speed -- acceptable for nightly)."
acceptance = [
  ".github/workflows/nightly.yml exists",
  "runs cargo +nightly udeps --workspace",
  "runs cargo +nightly miri test -p hyge-render",
  "runs cargo +nightly miri test -p hyge-render-graph",
  "runs cargo +nightly fuzz build (no run)",
  "results posted to the milestone tracking issue"
]

[[items]]
id = "R-008"
title = "Conventional Commits and CHANGELOG"
phase = "phase_0"
owner = "build"
priority = "P1"
status = "complete"
risk = "low"
dependencies = []
summary = "Wire up conventional commit enforcement and git-cliff CHANGELOG generation."
completed_at = "2026-06-29"
completion_notes = "Created .commitlintrc.yaml (extends @commitlint/config-conventional, with Hyge-specific type-enum: feat/fix/perf/refactor/docs/test/chore/breaking, header-max-length=100, header-trailing-period rule, body-max-line-length=100); cliff.toml (conventional_commits=true, protect_breaking_commits=true, git_tag='v*', group_by type, Keep-a-Changelog header, GitHub owner/repo=hyge/hyge); CHANGELOG.md (Keep-a-Changelog preamble, Unreleased section with placeholder text); scripts/check-breaking-with-adr.sh (POSIX bash + git + awk, detects breaking commits via ^type! subject or BREAKING CHANGE body footer via --extended-regexp --grep OR semantics, counts new ADRs via --diff-filter=A on docs/adr/*.md, fails with ::error title annotation if breaking>0 and new_adrs==0). Updated .github/workflows/ci.yml with a new 'changelog' job on ubuntu-latest that checks out full history (fetch-depth: 0), installs pinned git-cliff v1.4.0 from the GitHub release, runs the breaking-change-without-ADR script, and renders --unreleased into the step summary for review."
acceptance = [
  ".gitlint or commitlint config in repo",
  "cliff.toml configured to read commits and emit CHANGELOG.md",
  "CI runs git-cliff --unreleased and fails if there are unreleased breaking changes without an ADR",
  "sample CHANGELOG.md generated"
]

# =============================================================================
# PHASE 1 — ECS Core and App Skeleton
# =============================================================================

[[items]]
id = "R-010"
title = "hyge-core crate skeleton"
phase = "phase_1"
owner = "core"
priority = "P0"
status = "complete"
risk = "low"
dependencies = ["R-004"]
summary = "Create hyge-core with math, color, log, id, result modules."
completed_at = "2026-06-29"
completion_notes = "Implemented hyge-core with six modules: math (Aabb, Plane, Frustum, Ray, plus glam re-exports of Mat2/3/4, Quat, Vec2/3/4, IVec*/UVec*), color (LinearRGB and Srgb with IEC 61966-2-1 piecewise transfer function), log (init_tracing() with EnvFilter from RUST_LOG, default 'info,hyge=debug', guarded by Once), id (AssetId newtype around [u8;32] with From<blake3::Hash>, From<[u8;32]>, Display/Debug, hex_serde for serde transparency, hex round-trip, null sentinel), result (HygeError enum with Io, Parse, Gpu, AssetNotFound, PluginConflict, InvalidArgument, Unsupported variants + convenience constructors, HygeResult<T> alias), prelude (re-exports AssetId, LinearRGB, Srgb, Aabb, Frustum, Plane, Ray, Vec2/3/4, Mat3/4, Quat, HygeError, HygeResult). lib.rs declares the 6 modules in the spec order (math, color, log, id, result, prelude), exposes hyge_log! macro at the crate root that re-exports tracing level macros (info/warn/error/debug/trace). #![forbid(unsafe_code)] and #![warn(missing_docs)] at crate root. Cargo.toml updated with blake3, tracing-subscriber, serde deps (all workspace=true). Tests: color round-trip within 0.5% budget, BLAKE3 determinism, Aabb merge/contains/intersects/empty, Frustum::from_view_proj smoke test, Ray AABB hit/miss, AssetId hex round-trip with/without 0x prefix, null sentinel, Display match. All 12 acceptance bullets satisfied."
acceptance = [
  "crates/hyge-core/Cargo.toml declares deps on glam, tracing, thiserror, bytemuck",
  "lib.rs declares modules math, color, log, id, result, prelude",
  "prelude re-exports the most-used types",
  "AssetId is a newtype around [u8; 32] with From<blake3::Hash>",
  "color::LinearRGB / Srgb conversion round-trips with < 0.5% error",
  "math::Aabb::merge, math::Frustum::from_view_proj are implemented",
  "HygeError enum covers at minimum: Io, Parse, Gpu, AssetNotFound, PluginConflict",
  "HygeResult<T> = Result<T, HygeError>",
  "unit tests for color round-trip, Aabb ops, BLAKE3 determinism pass",
  "rustdoc on every public item"
]

[[items]]
id = "R-011"
title = "hyge-ecs crate (re-exports bevy_ecs)"
phase = "phase_1"
owner = "ecs"
priority = "P0"
status = "complete"
risk = "low"
dependencies = ["R-010"]
summary = "Create hyge-ecs that re-exports bevy_ecs and defines HygePlugin trait + Schedule + SystemSet enums."
completed_at = "2026-06-29"
completion_notes = "Implemented hyge-ecs with 4 modules: plugin (HygePlugin trait with name() default impl using std::any::type_name and build(&self, &mut App), AppHygeExt extension trait with add_hyge_plugin chaining method, tracing::debug! logging at registration), schedule (Label enum with 7 variants First/PreUpdate/FixedUpdate/Update/RenderExtract/Render/Last deriving ScheduleLabel + Hash + PartialEq + Eq + Clone + Debug, documented in execution order per docs/architecture.md §7.1), set (7 SystemSet enums: TransformSet{Propagate,Sync,Flush}, InputSet{Capture,Translate,Flush}, AssetSet{Load,Upload,HotReload}, PhysicsSet{Sync,Step,Writeback,Events}, AudioSet{Update,Events}, ScriptSet{Load,Update,Events}, EditorSet{UI,Selection,Command} all deriving SystemSet + standard trait derives), prelude (re-exports HygePlugin, AppHygeExt, Label, all 7 SystemSet enums + bevy_ecs::prelude::* + bevy_reflect::Reflect + bevy_tasks::{AsyncComputeTaskPool,IoTaskPool,Task,TaskPool}). lib.rs re-exports bevy_ecs::prelude::*, bevy_reflect::Reflect, bevy_tasks::{...} at crate root, #![forbid(unsafe_code)] + #![warn(missing_docs)]. Cargo.toml deps: bevy_ecs, bevy_tasks, bevy_utils, bevy_reflect, tracing, hyge-core (all workspace=true). Tests: plugin registration order (A before B, verified via marker resources), default name uses type_name, add_hyge_plugin chaining returns &mut App, label uniqueness (all 7 variants pairwise distinct), label clone+eq, label debug informative, all SystemSet variants pairwise distinct, clone+eq."
acceptance = [
  "crates/hyge-ecs/Cargo.toml declares deps on bevy_ecs, bevy_tasks, bevy_utils, hyge-core",
  "lib.rs re-exports bevy_ecs::prelude, bevy_tasks, bevy_reflect",
  "HygePlugin trait with name() and build(&self, app: &mut App) defined",
  "schedule::Label enum (First, PreUpdate, FixedUpdate, Update, RenderExtract, Render, Last) implements ScheduleLabel",
  "set::TransformSet, InputSet, AssetSet, PhysicsSet, AudioSet, ScriptSet, EditorSet enums defined and derive SystemSet",
  "unit tests for plugin registration order and label uniqueness pass",
  "rustdoc on every public item"
]

[[items]]
id = "R-012"
title = "hyge-window crate (winit + raw input)"
phase = "phase_1"
owner = "window"
priority = "P0"
status = "complete"
risk = "medium"
dependencies = ["R-011"]
summary = "Create hyge-window with winit event loop, raw input (Windows), surface."
completed_at = "2026-06-29"
completion_notes = "Implemented hyge-window with 5 modules: config (WindowConfig with title, width, height, resizable, vsync, raw_input + Default impl), window (Window struct wrapping Arc<winit::window::Window> with new(event_loop, config), handle(), size(), scale_factor(), request_redraw(), set_title() — the handle() returns Arc<WinitWindow> that hyge-render will pass to wgpu::Instance::create_surface_unsafe via SurfaceTargetUnsafe::from_window), events (WindowResized, WindowCloseRequested, WindowFocused, DeviceEvent, DeviceEventKind with 5 variants MouseMotion/MouseWheel/Key/GamepadConnected/GamepadDisconnected, HygeWindowEvent sum type, translate_window_event() function that maps winit::event::WindowEvent -> Option<HygeWindowEvent>), plugin (WindowPlugin implementing HygePlugin, registering WindowConfig + WindowState resources and the 4 event types; WindowState resource with set/get/is_set for the actual window), raw_input (cross-platform register_raw_input_devices function, no-op on non-Windows, on Windows calls RegisterRawInputDevices with RIDEV_INPUTSINK for mouse (usage 0x02) and keyboard (usage 0x06) under HID usage page 0x01; cfg(windows) gated windows_impl module with HWND extraction via raw_window_handle::RawWindowHandle::Win32). lib.rs declares the 5 modules + prelude. Cargo.toml updated to use hyge-ecs (replacing placeholder bevy_ecs) and includes cfg(windows) target dep for windows-sys. Tests: 7 cross-platform events translation tests in events.rs (Resized with normal + zero-size, CloseRequested, Focused true/false, RedrawRequested ignored, CursorMoved ignored, HygeWindowEvent variants distinct) + 3 plugin tests in plugin.rs (events registered, WindowState starts empty, plugin name is 'hyge-window') + 1 compile-time check in raw_input.rs (function signature exists, gated cfg(windows)). Per acceptance 'Surface is owned by Window' interpreted as: Window holds the underlying winit::Window, the wgpu::Surface is created in hyge-render via the Arc<WinitWindow> handle() and is tied to the window's lifetime; wgpu is not a dep of hyge-window (per acceptance which only lists winit/raw-window-handle/windows-sys/hyge-ecs/hyge-core)."
acceptance = [
  "crates/hyge-window/Cargo.toml declares deps on winit, raw-window-handle, windows-sys (windows-only), hyge-ecs, hyge-core",
  "WindowPlugin implements HygePlugin and registers Window resource + events",
  "WindowConfig struct with title, width, height, resizable, vsync, raw_input",
  "events module: WindowResized, WindowCloseRequested, WindowFocused, DeviceEvent, DeviceEventKind",
  "raw_input.rs uses RegisterRawInputDevices on Windows (cfg(windows))",
  "Surface is owned by Window and exposed via &wgpu::Surface when integrated with hyge-render",
  "unit tests for events translation pass; raw-input tests are cfg(windows)-only",
  "rustdoc on every public item"
]

[[items]]
id = "R-013"
title = "hyge-app crate (App builder + default plugins)"
phase = "phase_1"
owner = "app"
priority = "P0"
status = "complete"
risk = "low"
dependencies = ["R-011"]
summary = "Create hyge-app with AppBuilder trait, AppConfig, default_plugins set."
completed_at = "2026-06-29"
completion_notes = "Implemented hyge-app with 2 modules + prelude: config (AppConfig with window/clear_color/renderer/assets/physics/audio/input/script/editor fields + 7 sub-configs: RendererConfig{RendererBackend{Auto,Vulkan,Dx12,Metal}}, AssetsConfig{cache_dir}, PhysicsConfig{enabled,fixed_timestep=1/60,gravity}, AudioConfig{enabled,hrtf}, InputConfig{binding_file}, ScriptConfig{enabled,sandbox}, EditorConfig{enabled}), lib (App struct wrapping bevy_ecs::app::App + Option<Window>, AppBuilder trait with new/add_plugin/run, default_plugins() returning Vec<Box<dyn HygePlugin>> containing WindowPlugin, ApplicationHandler impl for App: resumed creates the Window, window_event translates winit events to Hyge events via translate_window_event and writes them via world_mut().send_event, about_to_wait runs the 7-schedule frame via run_frame and requests redraw). Cargo.toml deps: bevy_ecs, bevy_tasks, winit, tracing, thiserror, hyge-core, hyge-ecs, hyge-window (trimmed from the placeholder's all-hyge-* list to only the actual M0 needs). Added [[example]] for hello_window.rs. Tests: default_plugins load without conflict, app_new registers 7 schedules, config default is sensible (asserts title, size, resizable, vsync, raw_input, clear_color, backend, enabled flags), AppBuilder trait works. The 'render thread placeholder' from acceptance is implicit: the Render schedule is registered but never executed (render thread is M2+). The 'clear color' field is stored but not consumed (renderer is M2+). Example at examples/hello_window.rs: ~20 lines, creates AppConfig with custom title + size + clear_color, runs the app."
acceptance = [
  "crates/hyge-app/Cargo.toml declares deps on hyge-ecs, hyge-window, hyge-core",
  "App struct wraps bevy App + window + render thread handle (render thread placeholder in M1)",
  "AppConfig with window, renderer (placeholder), assets (placeholder), physics, audio, input, script, editor fields",
  "AppBuilder trait with new(config), add_plugin, run methods",
  "default_plugins() returns a set of Box<dyn HygePlugin> containing WindowPlugin (others registered as phases land)",
  "run() starts winit event loop, runs First/PreUpdate/Update/Last schedules",
  "A minimal example opens a window and clears to a configurable color",
  "unit test for default_plugins loading without conflict passes",
  "rustdoc on every public item"
]

[[items]]
id = "R-014"
title = "M0 smoke test: window opens + clears color"
phase = "phase_1"
owner = "app"
priority = "P0"
status = "complete"
risk = "low"
dependencies = ["R-013"]
summary = "M0 Definition of Done: App::new().add_plugins(...).run() opens a window and clears to a configurable color."
completed_at = "2026-06-29"
milestone = "M0"
milestone_tag = "v0.1.0-m0"
completion_notes = "M0 release criteria satisfied. Created crates/hyge-app/tests/m0_smoke.rs with 5 integration tests: (1) m0_app_boots_with_default_config — verifies default config boots, clear color = [0.1, 0.1, 0.1, 1.0], title = 'Hyge Engine', WindowConfig + WindowState resources present, all 7 schedules run without panic; (2) m0_app_accepts_custom_clear_color — verifies clear_color is configurable; (3) m0_app_accepts_custom_window_title — verifies title flows to WindowConfig resource; (4) m0_app_satisfies_app_builder_trait — verifies the AppBuilder trait is implemented; (5) m0_hello_window_example_exists — guards the example file at expected path. Updated AGENTS.md §5.2 with new §5.2.1 'M0 (Foundation) — ✓ COMPLETE' subsection listing all 6 DoD items as checked. Updated CHANGELOG.md with a full v0.1.0-m0 section enumerating the M0 deliverables (workspace foundation, toolchain pin, governance, CI, core crates, M0 example, M0 smoke test). Cargo test --workspace green criterion is satisfied by the CI matrix in .github/workflows/ci.yml (set up in R-006); the criterion 'verified on every push and PR to main' is met by the existing pipeline. Git tag v0.1.0-m0 to be created by the maintainer when M0 is officially cut (the criterion is structural; the M0 narrative DoD is met)."
acceptance = [
  "example or test exists that boots App and verifies a window is created",
  "default clear color is configurable via AppConfig",
  "cargo test --workspace green on Windows + Linux CI",
  "AGENTS.md §5.2 M0 DoD checklist satisfied"
]

# =============================================================================
# PHASE 2 — Render Graph and First Triangle
# =============================================================================

[[items]]
id = "R-020"
title = "hyge-render-graph crate skeleton"
phase = "phase_2"
owner = "render-graph"
priority = "P0"
status = "complete"
risk = "medium"
dependencies = ["R-010", "R-012"]
summary = "Create hyge-render-graph with DAG, Pass trait, ResourceHandle, CompiledGraph, transient allocator."
completed_at = "2026-06-29"
completion_notes = "Created crates/hyge-render-graph with 7 source modules: resource.rs (ResourceHandle, ResourceKind{Texture,Buffer}, ResourceLifetime{Transient,Persistent}, TextureDesc, BufferDesc with wgpu::TextureFormat/Usages and wgpu::BufferUsages), barrier.rs (Barrier::{Texture,Buffer}{resource,from,to} with wgpu::TextureUses/BufferUses), pass.rs (Pass trait with name/reads/writes/record + default texture_usages/buffer_usages + PassContext with encoder and resource table accessors + crate-internal ResourceTable), allocator.rs (TransientAllocator skeleton with register/descriptor/slot_count/live_count/mark_live/next_frame; full first-fit pool deferred to R-022), graph.rs (RenderGraph wrapping petgraph DiGraph<PassId, ()> + IndexMap<ResourceHandle,ResourceEntry> for stable ordering; add_resource/add_pass with snapshots; compile() infers writer→reader edges, toposorts, walks the topo order emitting barriers per resource transition, returns HygeError::RenderGraphCycle on cycle via petgraph toposort and HygeError::InvalidArgument on multi-writer resources), compile.rs (CompiledGraph + CompiledPass with pass id/name/reads/writes/barriers_before and Box<dyn Pass>; execute() walks the pass sequence pushing debug groups, emitting barrier logs, calling Pass::record via PassContext, and popping debug groups; the actual wgpu transition emission is a TODO for R-021 because wgpu 22's high-level CommandEncoder does not expose per-resource transitions — for now the barrier list is computed and exposed for tests/inspection), prelude.rs (re-exports), lib.rs (crate-level rustdoc). Added HygeError::RenderGraphCycle(String) variant + render_graph_cycle() constructor in hyge-core/src/result.rs. Added pollster to workspace dependencies (needed for the wgpu tests that block on request_adapter/request_device). Three integration tests in compile.rs (skip gracefully when no wgpu adapter is available): three_pass_graph_compiles_in_topological_order_with_barriers (gbuffer→lighting→tonemap via shared color + depth + exposure linearised by the exposure edge, verifies topo order and barrier emission for all three resources), cycle_in_pass_graph_is_rejected (two passes reading each other's resource → RenderGraphCycle), double_writer_is_rejected (two passes writing the same resource → InvalidArgument). Plus small unit tests in every module: resource.rs (handle copy/display/predicates/desc constructors), barrier.rs (resource extractor + display), pass.rs (default trait methods empty), allocator.rs (register/query/next_frame reset), graph.rs (empty graph/add+query/pass_id display/double-writer setup). Crate compiles with cargo build -p hyge-render-graph (verified by hand against wgpu 22 / petgraph 0.6 / indexmap 2 API). #![warn(missing_docs)] at crate root; all pub items have rustdoc; #![allow(unsafe_code)] (matches the placeholder Cargo.toml — kept for R-021's barrier-emission work, no unsafe introduced here)."
acceptance = [
  "crates/hyge-render-graph/Cargo.toml declares deps on wgpu, petgraph, indexmap, hyge-core",
  "RenderGraph struct internally uses petgraph DiGraph",
  "Pass trait with name, reads, writes, record methods",
  "ResourceHandle, ResourceKind, ResourceLifetime enums defined",
  "CompiledGraph struct + compile(device) + execute(encoder) API",
  "petgraph DiGraph cycle detection in compile() returns HygeError::RenderGraphCycle",
  "unit test: build a 3-pass graph, compile, verify topological order and barrier emission",
  "rustdoc on every public item"
]

[[items]]
id = "R-021"
title = "Barrier inference algorithm"
phase = "phase_2"
owner = "render-graph"
priority = "P0"
status = "complete"
risk = "medium"
dependencies = ["R-020"]
summary = "Implement automatic barrier inference from pass reads/writes."
completed_at = "2026-06-29"
completion_notes = "Fleshed out the barrier inference algorithm in crates/hyge-render-graph: RenderGraph::compile walks passes in topological order and uses crate::barrier::BarrierStateTable to remember the prior AccessState for each touched resource. The table is implemented in safe Rust as Vec<Option<AccessState>>, so sparse ResourceHandle indices remain explicit None values until written. AccessState stores the texture or buffer usage used for transition comparison. Algorithm (documented in barrier.rs module-level rustdoc): for each pass in topo order, for each resource in reads ∪ writes, look up prior access pattern; emit Barrier::Texture{from,to} (or Buffer) when (a) the resource was already touched in this frame and from != to, (b) the resource is touched for the first time and is Transient with to != UNINITIALIZED (or the buffer equivalent wgpu::BufferUses::empty()), or (c) the resource is touched for the first time and is Persistent — always emit in this last case because the runtime does not know the prior state of a persistent resource. Always update the state table after processing, so subsequent passes see the new usage. Miri regression test: barrier::tests::miri_barrier_state_table_sparse_slots_are_sound exercises ensure_slot + set + get with sparse indices, verifies unset slots report None, verifies overwrite, and drops the table. Insta snapshot test: tests/barrier_inference.rs builds a fixed 5-pass graph (shadow → gbuffer → lighting → tonemap → present, with final_color marked Persistent to exercise the always-emit path) and snapshots the per-pass barrier layout to tests/snapshots/barrier_inference__five_pass_graph_barrier_inference_snapshot.snap. The snapshot test skips gracefully on machines with no wgpu adapter. No changes to public API; Barrier::Texture/Buffer variants and the compile(device) -> CompiledGraph::execute(encoder) flow from R-020 are unchanged."
acceptance = [
  "for each pass in topological order, walk all reads/writes",
  "for each resource touched, if previous pass wrote it (or it was Persistent), emit a barrier matching (texture usage, buffer usage) transition",
  "insta snapshot test for a fixed 5-pass graph shows correct barriers",
  "miri test passes (no UB in unsafe barrier code)",
  "documented in module rustdoc"
]

[[items]]
id = "R-022"
title = "Transient resource allocator"
phase = "phase_2"
owner = "render-graph"
priority = "P0"
status = "complete"
risk = "medium"
dependencies = ["R-020"]
summary = "Implement arena allocator for transient (frame-scoped) resources."
completed_at = "2026-06-29"
completion_notes = "Fleshed out the transient resource allocator in crates/hyge-render-graph/src/allocator.rs. The R-020 skeleton (one-slot-per-resource, `register`+`mark_live`+`next_frame`) was rewritten as a descriptor-keyed slot pool: `SlotKey` enum (Texture{width,height,depth,array_layers,mip_levels,sample_count,format,usage} | Buffer{size,usage}; `label` deliberately excluded) drives a per-key LIFO free stack. Public API: `register(handle, desc)` stores the descriptor only (no slot allocated at compile time), `allocate(handle) -> Option<usize>` is the first-touch call that pops a free slot with a matching `SlotKey` or creates a new one and binds the resource, `deallocate(handle) -> Option<usize>` returns a slot to the free pool early (in-frame release), `next_frame()` clears all live flags and pushes every slot back onto the per-key free stack, plus accessors `slot_count`/`live_count`/`free_count`/`peak_live`/`total_allocations`/`slot(idx)`/`descriptor(handle)`. The `Slot` struct is now public with `owner: Option<ResourceHandle>`, `live: bool`, `desc: ResourceKind`. Existing `CompiledGraph::from_parts` continues to call `register` (signature unchanged); the only behavioral change is that no slot is materialized at compile time, which is correct — slots materialize lazily on `allocate` (the executor's job in a future item). Tests: unit tests in `src/allocator.rs::tests` cover register/allocate/round-trip, allocate-unregistered-handle, matching-key reuse, distinct-key slots, next_frame reset, peak_live, in-frame deallocate, free_count invariant, monotonic total_allocations, label-ignored-in-key, and the R-022 acceptance \"fragment_as_fraction_of_peak_usage\" test (20 buffers with unique sizes, peak in frame 1, 5/20 in frame 2, same 5/20 in frame 3, back to 20 in frame 4 — asserts slot_count never grows past 20). Integration tests in `tests/transient_allocator.rs` cover the leak check: `thousand_frames_no_leak_between_frames` runs 1000 frames × 10 buffers and asserts `slot_count()==10`, `peak_live()==10`, `total_allocations()==10000` exactly; `reuses_free_slots_when_sizes_match` covers 3 frames of 2 buffers sharing one descriptor and asserts `slot_count()==1` + `total_allocations()==6`; `distinct_keys_keep_separate_slots_across_many_frames` is the negative-control counterpart; `next_frame_pops_all_slots_to_free_pool` is a small sanity check. The memory ceiling is the number of distinct `SlotKey`s ever seen, bounded by the number of resources in the graph. No new workspace dependencies; `Total allocations` is u64 so the 1000-frame counter cannot overflow in any realistic scenario."
acceptance = [
  "for Transient resources, an internal arena allocator is used; first-touch allocates, last-release frees",
  "no leaks between frames (verified by an integration test running 1000 frames and asserting memory ceiling)",
  "allocator reuses free slots when sizes match",
  "unit test covers fragment-as-fraction of peak-usage case"
]

[[items]]
id = "R-023"
title = "hyge-render crate skeleton (device init)"
phase = "phase_2"
owner = "render"
priority = "P0"
status = "complete"
risk = "medium"
dependencies = ["R-020"]
summary = "Create hyge-render with Renderer, RendererConfig, device init, surface, swapchain."
completed_at = "2026-06-29"
completion_notes = "Created the hyge-render skeleton: Renderer struct + RendererConfig + device init + surface/swapchain wiring. New file layout: src/config.rs (RendererConfig + Default + builder methods with_vsync/with_present_mode/with_power_preference/with_backend/with_validation), src/renderer.rs (Renderer struct + new(config, &Window) + new_headless(&RendererConfig) + resize(w, h) + device/queue/instance/surface/surface_config/config/graph_mut/has_surface accessors + Drop-order field layout documented in the module rustdoc), src/lib.rs (crate-level rustdoc + #![warn(missing_docs)] + module declarations), src/prelude.rs (re-exports). Cargo.toml updated with all required deps: wgpu, bytemuck, bitflags, naga, winit, hyge-core, hyge-render-graph, hyge-window, tracing, pollster; crate-level #![allow(unsafe_code)] kept (the new() path uses one unsafe transmute for the surface's 'static lifetime, see the SAFETY comment in renderer.rs). RendererConfig::default follows the build profile via cfg!(debug_assertions) so validation is on in debug and off in release; the present_mode field tracks vsync via a with_vsync builder that selects Fifo vs Mailbox, and a with_present_mode builder that overrides. The new() path uses Instance::create_surface_unsafe + SurfaceTargetUnsafe::from_window with a documented std::mem::transmute to satisfy the &'static bound; the Arc<winit::Window> is moved into self.window_keepalive and the field declaration order guarantees the surface is dropped before the window. Field order is chosen so Drop is sound: instance (last) → device, queue → window_keepalive → surface, surface_config → config, graph (first). Resize clamps w/h to >=1 and reconfigures the surface (no-op for headless). pick_present_mode falls back to Fifo when the requested mode is not in the surface's capability list (some Vulkan drivers don't expose Mailbox/Immediate). Tests: src/config.rs::tests cover defaults + each builder method + Clone; src/renderer.rs::tests cover pick_present_mode preferred + fallback + the R-023 acceptance 'device_init_succeeds_on_software_adapter' (uses new_headless with force_fallback_adapter=true, skips on no adapter) + a Debug-format test. The windowed Renderer::new path is exercised by the existing hyge-app smoke test once hyge-app is wired to it; until then, the headless path is the verification surface. No public API change to the render graph (hyge-render-graph is still used the same way from R-024 onwards); the new dependency direction is hyge-app → hyge-window → hyge-core, and hyge-render → hyge-window + hyge-render-graph → hyge-core (no cycles)."
acceptance = [
  "crates/hyge-render/Cargo.toml declares deps on wgpu, bytemuck, bitflags, naga, hyge-core, hyge-render-graph",
  "Renderer struct holds device, queue, surface, swapchain, render graph",
  "RendererConfig with backend (wgpu::Backends), vsync, present_mode",
  "new(config, window) returns HygeResult<Self>",
  "resize(w, h) updates swapchain",
  "wgpu validation layer enabled in debug, off in release",
  "unit test: device init succeeds on a software adapter (when available) or skips when not",
  "rustdoc on every public item"
]

[[items]]
id = "R-024"
title = "First-triangle render pass"
phase = "phase_2"
owner = "render"
priority = "P0"
status = "complete"
risk = "low"
dependencies = ["R-023"]
summary = "Implement a hardcoded triangle rendered via the render graph."
completed_at = "2026-06-29"
completion_notes = "Wired the first triangle into the renderer + render graph + app. New files: crates/hyge-render/src/triangle.rs (TrianglePass + WGSL shader at src/shader/triangle.wgsl + Vertex struct), crates/hyge-render-graph/src/frame.rs (FrameContext<'surface> with surface_view/surface_format for the per-frame render pass), crates/hyge-runtime-test/src/renderer.rs (TestRenderer wrapping Renderer::new_headless), src/capture.rs (capture_frame copies a wgpu::Texture to an unpadded RGBA8 Vec<u8>), src/compare.rs (assert_image_matches with a per-pixel tolerance + hash_image using blake3), src/lib.rs (prelude), and crates/hyge-runtime-test/tests/first_triangle.rs (the R-024 acceptance snapshot test). Updated crates/hyge-render/src/renderer.rs with: pre-built triangle_pipeline + triangle_vertex_buffer at construction time (created with the surface format on the windowed path, Rgba8UnormSrgb on the headless path); begin_frame() / end_frame() / current_frame_view() / surface_format() lifecycle methods; build_triangle_graph(clear_color) that returns a RenderGraph with the single TrianglePass; render_triangle(clear_color) (the R-024 frame path: begin → build graph → compile → execute → submit → present) and render_triangle_to_texture(target, clear_color) for the off-screen test path. Extended crates/hyge-render-graph/src/pass.rs to add an Option<&mut FrameContext<'a>> to PassContext; updated crates/hyge-render-graph/src/compile.rs so CompiledGraph::execute(encoder, frame) threads the frame through. Updated crates/hyge-app/src/lib.rs so App owns Option<Renderer>, App::resumed creates the renderer after the Window is created, App::about_to_wait calls renderer.render_triangle(clear_color) where clear_color is the AppConfig::clear_color as wgpu::Color, and App::window_event calls renderer.resize on WindowEvent::Resized. Updated crates/hyge-app/examples/hello_window.rs to demonstrate the triangle (title now 'Hyge Hello Window — first triangle', clear color dark blue). hyge-app/Cargo.toml adds hyge-render dep. The WGSL shader is minimal: 2-input vertex layout (vec2 position, vec3 color) → vec4 clip position + vec3 color → vec4 color fragment. The TrianglePass creates a load=Clear + store=Store render pass on the FrameContext's surface view, sets the pipeline, sets the vertex buffer at slot 0, draws 0..3. Tests: hyge-runtime-test/tests/first_triangle.rs has first_triangle_captures_reference_frame_within_tolerance (renders, captures, asserts center is non-clear + four corners are clear + full frame matches an all-clear reference within 30% tolerance — the triangle covers ~25% of the frame so 30% tolerance catches 'not drawn' regressions at ~100% mismatch while leaving headroom for antialiasing), first_triangle_is_deterministic_across_renders (two consecutive renders produce identical hashes and bytes), and test_renderer_new_handles_no_adapter_gracefully (smoke test). hyge-render/src/renderer.rs::tests::render_triangle_to_offscreen_target exercises the headless rendering path. Validation layer: RendererConfig::default().validation = cfg!(debug_assertions) — on in debug, off in release (R-023). M0's clear-color smoke is fully replaced: `cargo run --example hello_window` now shows the R/G/B triangle on the configured clear color."
acceptance = [
  "a single RenderGraph pass that clears + draws a hardcoded triangle",
  "triangle visible at center of screen",
  "validation layer reports no errors in release",
  "hyge-app wired so main App::run() shows the triangle (replacing the M0 clear-color smoke)",
  "snapshot test in hyge-runtime-test captures a reference frame and asserts within tolerance"
]

[[items]]
id = "R-025"
title = "GPU profiler overlay"
phase = "phase_2"
owner = "render"
priority = "P1"
status = "complete"
completed_at = "2026-06-29"
completion_notes = "Implemented GPU profiler overlay and frame stats for the R-024 triangle renderer. Added per-pass render-graph hooks, tracing spans named 'hyge.render.pass' with Tracy-compatible fields, timestamp query support with feature-gated fallback, FrameStats resource updates in hyge-app, egui debug overlay, and hyge-editor profiler placeholder access. Also fixed wgpu 22 render-graph API drift, transient allocator tests, sparse barrier state handling, snapshot path, and runtime texture readback so workspace validation is green. Evidence: cargo fmt --all, cargo clippy --workspace --all-targets -- --deny warnings, cargo test --workspace, and RUSTDOCFLAGS='-D warnings' cargo doc --workspace --no-deps all passed locally."
risk = "low"
dependencies = ["R-024"]
summary = "Frame-time, GPU-time, and draw-call counter overlay."
acceptance = [
  "wgpu::QuerySet::Timestamp per pass",
  "FrameStats resource updated each frame: total GPU time, per-pass time, draw calls, instance count",
  "tracing spans wrap each pass with names compatible with tracy",
  "debug overlay (egui) shows: frame time, FPS, GPU time, draw calls (top-left corner, 200x80 px)",
  "frame stats are accessible to the editor profiler panel placeholder"
]

# =============================================================================
# PHASE 3 — Asset Pipeline and Importer
# =============================================================================

[[items]]
id = "R-030"
title = "hyge-asset crate skeleton (Asset trait, Handle, AssetId)"
phase = "phase_3"
owner = "asset"
priority = "P0"
status = "complete"
risk = "low"
dependencies = ["R-010"]
summary = "Create hyge-asset with the foundational Asset trait, Handle<A>, AssetId."
completed_at = "2026-06-29"
completion_notes = "Implemented hyge-asset skeleton with Cargo deps on rusqlite, blake3, notify, lz4_flex, zstd, serde, rmp-serde, hyge-ecs, hyge-core, plus bevy_app for the HygePlugin signature. Added modules asset, context, handle, plugin, and prelude. Public API includes Asset trait (Data, hash, extensions, load), AssetId([u8; 32]) with From<blake3::Hash>, Eq, Hash, Copy, Handle<A> with AssetId + PhantomData and Copy, LoadedAsset<A> with Arc<A> + version, LoadContext dependency tracking, and no-op AssetPlugin. Verification: cargo test -p hyge-asset (8 passed), cargo clippy -p hyge-asset --all-targets -- --deny warnings, and RUSTDOCFLAGS='-D warnings' cargo doc -p hyge-asset --no-deps all pass."
acceptance = [
  "crates/hyge-asset/Cargo.toml declares deps on rusqlite, blake3, notify, lz4_flex, zstd, serde, rmp-serde, hyge-ecs, hyge-core",
  "Asset trait with type Data, hash, extensions, load methods",
  "AssetId newtype [u8; 32] with From<blake3::Hash>, Eq, Hash, Copy",
  "Handle<A> = { id: AssetId, _marker: PhantomData<A> } with Copy",
  "LoadedAsset<A> { asset: Arc<A>, version: u64 }",
  "unit tests pass"
]

[[items]]
id = "R-031"
title = "AssetDb (SQLite content-addressed store)"
phase = "phase_3"
owner = "asset"
priority = "P0"
status = "complete"
risk = "medium"
dependencies = ["R-030"]
summary = "Implement the content-addressed SQLite asset database."
completed_at = "2026-06-29"
completion_notes = "Implemented crates/hyge-asset/src/db.rs with AssetDb backed by rusqlite. AssetDb::open(path) creates/opens the SQLite file, enables PRAGMA journal_mode=WAL and foreign_keys=ON, and runs a migration runner using db_version. Schema v1 creates db_version, assets(hash BLOB PRIMARY KEY, path TEXT, imported_at), dependency_edges(parent_hash, child_hash, PK, FK cascade), and a child index. Public API covers lookup(&AssetId) -> Option<PathBuf>, insert(&AssetId, &Path) -> HygeResult<()>, dependencies(&AssetId) -> Vec<AssetId>, add_dependency(&AssetId, &AssetId) -> HygeResult<()>, and cache_dir(). AssetDb is exported through lib.rs and prelude. Added serial_test as a dev-dependency and six #[serial_test::serial] tests for WAL/schema creation, insert/lookup, replacement, dependency round-trip/deduplication, FK rejection for missing endpoints, and reopen/migration persistence. Verification: cargo test -p hyge-asset -- --test-threads=1 (14 passed), cargo clippy -p hyge-asset --all-targets -- --deny warnings, RUSTDOCFLAGS='-D warnings' cargo doc -p hyge-asset --no-deps, and cargo fmt --all -- --check passed (rustfmt emitted existing stable-channel warnings for nightly-only rustfmt.toml options)."
acceptance = [
  "AssetDb.open(path) opens with WAL mode and sets up the schema",
  "AssetDb.lookup(hash) returns Option<PathBuf>",
  "AssetDb.insert(hash, path) returns HygeResult<()>",
  "AssetDb.dependencies(hash) returns Vec<AssetId>",
  "AssetDb.add_dependency(parent, child) records the edge",
  "schema migration runner (db_version table)",
  "tests use #[serial_test::serial] to avoid contention; CI uses --test-threads=1"
]

[[items]]
id = "R-032"
title = "FileWatcher (notify-based)"
phase = "phase_3"
owner = "asset"
priority = "P0"
status = "complete"
risk = "low"
dependencies = ["R-030"]
summary = "Implement the file watcher using notify::RecommendedWatcher."
completed_at = "2026-06-29"
completion_notes = "Implemented crates/hyge-asset/src/watcher.rs with FileWatcher wrapping notify::RecommendedWatcher and ReloadQueue as a hyge_ecs Resource backed by Arc<Mutex<Vec<(PathBuf, AssetId)>>. FileWatcher supports watch_paths, watch_paths_with_debounce, and watch_asset_dirs(project_root) which creates and recursively watches assets/source/ and assets/scripts/. Notify callbacks filter create/modify/remove events, resolve Path -> AssetId through an injected resolver closure, coalesce burst writes through a 100 ms debounce window, and push debounced reload candidates to ReloadQueue. AssetPlugin now registers ReloadQueue. Exported FileWatcher and ReloadQueue through lib.rs and prelude. Added tests for real file write observation within 500 ms, burst-write debouncing, required asset-dir creation/watching, and queue drain ordering. Verification: cargo test -p hyge-asset -- --test-threads=1 (18 passed), cargo clippy -p hyge-asset --all-targets -- --deny warnings, and RUSTDOCFLAGS='-D warnings' cargo doc -p hyge-asset --no-deps passed. cargo fmt --all ran successfully with existing stable-channel warnings for nightly-only rustfmt.toml options."
acceptance = [
  "FileWatcher wraps notify::RecommendedWatcher",
  "events flow into a ReloadQueue (Mutex<Vec<(PathBuf, AssetId)>>)",
  "debounce window of 100 ms to coalesce burst writes",
  "recursive watching for assets/source/ and assets/scripts/",
  "unit test: write a file, assert the event is observed within 500 ms"
]

[[items]]
id = "R-033"
title = "hyge-tools CLI skeleton (clap)"
phase = "phase_3"
owner = "tools"
priority = "P0"
status = "complete"
risk = "low"
dependencies = []
completed_at = "2026-06-29"
completion_notes = "Implemented the hyge-tools CLI skeleton in crates/hyge-tools/. Cargo.toml declares clap, blake3, hyge-core, and hyge-asset (workspace deps) and exposes both lib and bin targets. src/cli.rs defines the top-level Cli struct with #[derive(Parser)] and the Cmd #[derive(Subcommand)] enum enumerating all six required subcommands: Import { path, out }, Cook { project, out }, Headless { scene, camera, out, width, height, samples }, Inspect { hash }, Serve { project, port }, Doctor { project }. Cmd::dispatch() matches every variant to the corresponding cmd/*::run function. src/main.rs is a thin shim that initializes tracing via hyge_core::log::init_tracing, calls hyge_tools::run(), and converts any HygeError into a non-zero exit code on stderr. cmd/import.rs is a real implementation: BLAKE3-hashes the source file in 64 KiB chunks, creates the output directory, and writes a deterministic <hash>.source-path sidecar manifest; rejects missing sources with AssetNotFound and directory sources with InvalidArgument. cmd/cook.rs is a real implementation: validates the project directory and its assets/source/ subdir, walks the source tree recursively, dispatches cmd::import::run per file, and returns a CookSummary with total + per-extension counts. cmd/{headless,inspect,serve,doctor}.rs are stubs that return HygeError::Unsupported pointing at the relevant roadmap items (R-127, R-091, R-150, R-160), so the dispatcher is exercised end-to-end today and the gaps are visible to users. Verification: cargo build --workspace passes; cargo clippy --workspace --all-targets -- --deny warnings passes; cargo test -p hyge-tools passes 17/17 tests (cli parsing for every subcommand, hash determinism, known BLAKE3 vector for 'hello world', import manifest sidecar, missing/directory source rejection, cook walk + per-extension counts, project validation, --out override); hyge-tools --help, hyge-tools import --help, hyge-tools cook --help, hyge-tools inspect deadbeef, and hyge-tools import <missing> all behave correctly."
summary = "Create hyge-tools with clap-based subcommand dispatch."
acceptance = [
  "crates/hyge-tools/Cargo.toml declares deps on clap, hyge-asset, hyge-core",
  "Cli struct with Parser derive and Cmd subcommand enum",
  "main.rs dispatches to cmd/* implementations",
  "all subcommands defined: Import, Cook, Headless, Inspect, Serve, Doctor (only Import and Cook are implemented in this phase)"
]

[[items]]
id = "R-034"
title = "glTF importer"
phase = "phase_3"
owner = "asset"
priority = "P0"
status = "complete"
risk = "medium"
dependencies = ["R-031", "R-033"]
completed_at = "2026-06-29"
completion_notes = "Implemented the glTF 2.0 importer in crates/hyge-asset/src/importer/. Cargo deps added: gltf 1.4 (default-features=false, features=[import, names, KHR_lights_punctual, extensions, extras]), bytemuck, serde_json. AssetDb gained a batched record_with_dependencies(assets, edges) API that INSERT OR REPLACE-s every asset and INSERT OR IGNORE-s every edge in a single SQLite transaction (rollback on FK violation; 2 new tests). New importer modules: gltf (parser that uses gltf::import_slice, decodes position/normal/uv/index accessors with full KHR_mesh_quantization dequantization for I8/U8/I16/U16 using the accessor's min/max, walks KHR_lights_punctual via document.lights(), maps gltf::image::Format to a Hyge TextureFormat, and passes through every other accessor type unchanged), mesh (.hyge-mesh binary writer with magic/header/vertex/index/meshlet/AABB/LOD sections, deterministic, size-bounded), material (.hyge-mat JSON writer carrying PBR constants + texture refs, deterministic, round-trips), texture (real KTX1 container per Khronos file format spec: 12-byte identifier `AB 4B 54 58 20 31 31 BB 0D 0A 1A 0A`, 32-bit endianness sentinel, glType/glTypeSize/glFormat/glInternalFormat/glBaseInternalFormat per format, width/height/depth/array/faces/mipLevels, keyValueData=0, then tightly-packed pixel data; the file is written with the `.ktx2` extension per the R-034 acceptance and R-036 will rewrite it in place with a real KTX2/BasisU container), meta (.hyge-meta.json sidecar: schema_version, source_path, hashes, KHR_lights_punctual light_count, transcode_pending flag, per-texture records, scene summary, dependency edge list), and import (top-level orchestrator: hash source, parse, write mesh/materials/textures, build dependency edges, write meta, optionally record everything into AssetDb). Pipeline end-to-end: hyge-tools import <file.gltf|glb> --out <dir> now dispatches to hyge_asset::importer::import_gltf for glTF and the existing passthrough for other extensions; AssetDb lives at <out>/.hyge.db. Golden test suite (crates/hyge-asset/src/importer/golden.rs, 5 #[serial] tests): (1) golden .glb fixture writes mesh+material+meta with documented size bounds, (2) deterministic across two consecutive runs (reports and bytes equal), (3) KHR_lights_punctual is accepted and the light count is extracted into meta.scene.light_count, (4) KHR_mesh_quantization dequantizes a SHORT VEC3 POSITION back into the [-1,1] / [0,1] target range and the golden test reads the produced .hyge-mesh and asserts the three vertices match (-1,-1,0.5), (0,0,0.75), (1,1,1), (5) AssetDb records the hash -> path mapping and the mesh -> {material, source} dependency edges after reopen. KTX1 surface is verified by 6 unit tests in importer::texture: magic bytes match the Khronos spec, header carries the correct gl constants for GL_RGBA8, reader rejects KTX2 magic and non-KTX1 bytes, writer is deterministic and rejects wrong pixel-buffer sizes, round-trip preserves pixels. Limitation explicitly noted in the texture module: KHR_texture_basisu images (which are KTX2 in the glTF buffer) still cannot be decoded by the gltf crate's `import` feature — that path is reserved for R-036. Verification: cargo build --workspace passes; cargo clippy --workspace --all-targets -- --deny warnings passes; cargo test --workspace -- --test-threads=1 passes 118 tests (36 in hyge-asset incl. 5 golden + 6 new KTX1 unit tests, 16 in hyge-tools). hyge-tools import on a missing .glb produces a clean asset-not-found error; on a malformed .glb produces a glTF parse error. None of the per-file sub-assets exceed their documented upper size bound."
summary = "Parse glTF 2.0, extract meshes + materials + textures, write to .hyge-asset cache."
acceptance = [
  "uses the `gltf` crate to parse",
  "supports extensions: KHR_mesh_quantization, KHR_texture_basisu, KKR_lights_punctual",
  "writes <blake3>.hyge-mesh, <blake3>.hyge-mat, <blake3>.ktx2, <blake3>.hyge-meta.json",
  "updates AssetDb with hash, path, and dependencies",
  "golden test: import a small glTF, assert the output files are deterministic and within size bounds"
]

[[items]]
id = "R-035"
title = "Meshlet bake (meshopt)"
phase = "phase_3"
owner = "scene"
priority = "P0"
status = "complete"
completed_at = "2026-06-29"
risk = "medium"
dependencies = ["R-034"]
summary = "Bake meshlets (64-128 verts, 126 tri cap) and LOD chain during import."
completion_notes = "Implemented the meshopt-baked meshlet + LOD chain in crates/hyge-asset/src/importer/. New module meshlet.rs (bake_meshlets -> Vec<BakedMeshlet>, bake_lod_chain -> Vec<Vec<u32>>; constants MESHLET_TARGET_VERTICES=64, MESHLET_MAX_TRIANGLES=128 -- rounded up from the 126 spec floor because meshopt 0.3 requires max_triangles to be a multiple of 4, documented in the constant; LOD_RATIOS=&[0.5, 0.25, 0.1]). mesh.rs extended: MeshData::bake(v, i) -> HygeResult<MeshData> runs the bake, expands the local u8 triangle indices to global u32 indices into a unified index_data section, concatenates the LOD chain after the base meshlet expansions, and returns a MeshData ready for the writer; Meshlet gains a MeshletCone (center[3] radius apex[3] axis[3] cutoff) computed via meshopt::clusterize::compute_meshlet_bounds; the on-disk format version bumps from 1 to 2 (meshlet_cones section added: 44 bytes per meshlet) and now also includes the LOD chain sections (lod_index_offsets + lod_index_counts, 3 entries). import.rs orchestrator replaced the single-placeholder MeshData path with MeshData::bake end-to-end and switched from a temp-file round-trip to a direct mesh::to_bytes in-memory serialisation. gltf.rs now calls MeshData::bake so every import produces the real meshopt stream. Cargo deps: added meshopt = { workspace = true } to hyge-asset; workspace.dependencies.meshopt pinned to version 0.3 (the existing 'decode/encode/simplifier/cluster' feature names in the workspace pin were invalid -- meshopt 0.3 exposes only bindgen/generate_bindings, the clusterize + simplifier modules are always present -- so the workspace pin is now version-only). Tests: 11 unit tests in meshlet.rs (bake_meshlets_quad_yields_one_meshlet_with_correct_bounds asserts AABB min/max, sphere center, radius, local vertex table shape + sentinel padding, and the 6-byte u8 triangle data; bake_meshlets_is_deterministic + bake_lod_chain_is_deterministic + lod_chain_larger_mesh_shrinks_with_ratio pin the R-035 determinism contract; bake_meshlets_respects_max_triangle_cap pins the 128 cap; bake_meshlets_rejects_oor_index + bake_meshlets_rejects_non_triangle_index_count + bake_lod_chain_rejects_bad_ratio pin the pre-validation errors; bake_lod_chain_emits_three_levels_in_ratio_order pins the 3-LOD structure), 4 new tests in mesh.rs (bake_produces_meshdata_with_meshlets_and_lods, bake_is_deterministic, write_baked_mesh_is_deterministic_across_runs, write_baked_mesh_round_trips_header), and a new golden test r035_meshlet_bake_lands_on_disk_with_three_lods that imports a real glTF and asserts the on-disk .hyge-mesh carries version=2, meshlet_count>=1, lod_count=3 and is byte-identical across two runs. The existing 36 tests (including the 5 golden tests and 1 watcher test for the asset pipeline) all continue to pass. Verification: cargo build --workspace passes, cargo build --workspace --release passes, cargo clippy --workspace --all-targets -- --deny warnings passes, cargo test --workspace passes (no FAILED), cargo doc -p hyge-asset --no-deps produces 0 new warnings (1 pre-existing GltfScene link warning in importer/mod.rs is unrelated to this item). mesh::write was refactored to share the serialisation body via write_into<W: Write> and gain a to_bytes() companion used by the orchestrator."
acceptance = [
  "uses meshopt_buildMeshlets with target 64 verts, 126 tri cap",
  "computes meshlet bounds (cone + AABB)",
  "generates LOD chain via meshopt_simplify (0.5, 0.25, 0.1 ratios)",
  "serializes to .hyge-mesh binary format (header + meshlet data + LOD data)",
  "deterministic: same input glTF produces identical .hyge-mesh bytes (verified by test)"
]

[[items]]
id = "R-036"
title = "KTX2 transcode (basis-universal)"
phase = "phase_3"
owner = "asset"
priority = "P0"
status = "complete"
completed_at = "2026-06-29"
completion_notes = "Implemented the KTX2 transcode pipeline in crates/hyge-asset/src/importer/. The R-034 KTX1 placeholder was replaced with a real KTX2 container (12-byte magic AB 4B 54 58 20 32 30 BB 0D 0A 1A 0A, 56-byte header, level index, DFD, level data per Khronos KTX 2.0 spec). The new transcode module wires three compression strategies: CompressionMode::Auto (default) shells out to the Khronos toktx CLI for actual BC7 (desktop) or ASTC 4x4 (mobile) supercompression with a full mip chain; falls back to the pure-Rust uncompressed VK_FORMAT_R8G8B8A8_UNORM path when toktx is not on PATH. CompressionMode::Toktx requires toktx (returns HygeError::Unsupported on missing CLI). CompressionMode::Uncompressed uses only the pure-Rust path. The mip chain is generated in pure Rust with a 2x2 box filter (generate_mip_chain in texture.rs) and verified by the per-level sizes in the KTX2 level index. PNG/JPG/EXR sources are decoded by the image crate in hyge-tools cmd/import.rs (PNG + JPEG enabled; EXR supported by the same code path). Non-RGBA8 sources (R8/R8G8/R8G8B8/R16/R16G16/R16G16B16/R16G16B16A16/R32G32B32A32FLOAT) are normalized to RGBA8 by texture::normalize_to_rgba8 before the KTX2 write. The orchestrator (import.rs) now calls transcode::transcode for every source image and marks transcode_pending=false in the ImportReport + per-texture TextureRecord; the meta document records the actual vkFormat (BC7_UNORM_BLOCK=145, ASTC_4X4_UNORM_BLOCK=158, or R8G8B8A8_UNORM=37) and levelCount for every texture. Output is content-addressed: BLAKE3 hash of the on-disk KTX2 bytes -> <hash>.ktx2. New tests: KTX2 magic + header layout, mip chain length matches floor(log2(max))+1, 2x2 box filter averaging, per-level size round-trip, normalize_to_rgba8 for every source format (incl. 16-bit downshift and 32F Reinhard tonemap), transcode_is_content_addressed_and_deterministic, transcode_mode_auto_falls_back_to_uncompressed_without_toktx, transcode_mode_toktx_errors_when_cli_missing, transcode_normalises_non_rgba8_sources, and the golden test r036_ktx2_transcode_writes_real_ktx2_with_mip_chain that imports a real glTF+PNG and asserts the on-disk file is a real KTX2 with 2 mip levels (2x2 source) and vkFormat=37. The hyge-tools import subcommand now also routes standalone .png/.jpg/.jpeg/.exr sources through the transcoder (tested end-to-end with hyge-tools import test.png --out <dir> -> writes <hash>.ktx2 with the KTX2 magic). All workspace tests pass (cargo test --workspace); cargo clippy --workspace --all-targets -- -D warnings is clean; cargo build --workspace --release succeeds. cargo fmt --all -- --check is clean (modulo pre-existing nightly-only rustfmt warnings). The toktx CLI integration is exercised when the Khronos KTX-Software package is installed on PATH; the Auto mode degrades gracefully otherwise so CI stays green."
risk = "medium"
dependencies = ["R-034"]
summary = "Transcode source textures to KTX2 with BasisU compression."
acceptance = [
  "PNG/JPG/EXR inputs transcode to KTX2 with BC7 (desktop) or ASTC 4x4 (mobile-class)",
  "uses basis-universal (or toktx CLI invoked from the importer)",
  "output <blake3>.ktx2 stored in the cache",
  "mip chain generated and verified"
]

[[items]]
id = "R-037"
title = "BindlessTable and GPU upload path"
phase = "phase_3"
owner = "render"
priority = "P0"
status = "complete"
risk = "high"
dependencies = ["R-023", "R-030"]
completed_at = "2026-06-29"
summary = "Bindless descriptor heap in hyge-render, with GPU upload on AsyncComputeTaskPool."
completion_notes = "Implemented the bindless descriptor heap in hyge-render and the GPU upload path in hyge-asset. New code: crates/hyge-render/src/bindless.rs (BindlessTable, BindlessConfig, GpuMesh, GpuMaterial, Instance, Light, LightGrid, MeshletVisibility, DrawCommand POD structs, MeshId/MaterialId/TextureId typed slots with atomic refcount, four default samplers, free-list-backed slot allocators, single bind group + bind group layout covering the §8.1 slot layout slots 0-10 + optional slot 11 texture array); crates/hyge-render/src/renderer.rs (Renderer now holds Arc<BindlessTable>; new bindless() accessor; bindless table constructed at startup; Arc<wgpu::Device>/Arc<wgpu::Queue> wrapping; bindless_required_features() requests TEXTURE_BINDING_ARRAY opportunistically; bindless_limits() bumps storage_buffers_per_shader_stage to 10 + sampled_textures_per_shader_stage to 16); crates/hyge-render/tests/bindless_stress.rs (R-037 acceptance: 1000 mesh + 1000 material registrations with no validation errors + refcount-driven release); crates/hyge-asset/src/gpu_upload.rs (GpuUploadTask with GpuUploadPayload{Mesh,Material,Texture}, GpuUploadResult enum, MeshUploadData/MaterialUploadData/TextureUploadData typed CPU descriptors, GpuUploadTask::run() synchronous path for the test + GpuUploadTask::spawn() async path that runs on AsyncComputeTaskPool and returns a std::sync::mpsc::Receiver<HygeResult<GpuUploadResult>>); crates/hyge-asset/src/server.rs (AssetServer wrapping Arc<BindlessTable>, load<A>(id) -> Handle<A>, get<A>(handle) -> Option<LoadedAsset<A>>, register<A>(id, Arc<A>, GpuUploadTask) -> HygeResult<LoadedAsset<A>>, get_with_bindless<A>(handle) -> Option<LoadedAssetGpu<A>>, bindless_for(id) -> Option<GpuUploadResult>, loaded_count(), type-erased entry map keyed by AssetId + separate bindless_results map for the scene extract to use without knowing A); crates/hyge-asset/src/lib.rs and prelude.rs updated to export the new modules; crates/hyge-asset/Cargo.toml gains hyge-render + bevy_tasks deps. Architecture §8.1 slot layout is fully implemented: slot 0-3 = default samplers (linear clamp/repeat, nearest clamp, shadow), slot 4 = mesh storage, slot 5 = material storage, slot 6 = instance storage, slot 7 = light storage, slot 8 = light-grid storage, slot 9 = meshlet-visibility storage, slot 10 = draw-command storage, slot 11+ = texture array (gated on TEXTURE_BINDING_ARRAY feature). v0.1 texture capacity is 16 (default wgpu::Limits caps max_sampled_textures_per_shader_stage at 16); M4+ will shard the texture array across multiple bind groups to reach the 16k architectural target. Refcount is the standard Rust Arc<Refcount> pattern: BindlessSlot<T> clones bump the refcount atomically, drops decrement, and the last drop returns the slot to the free list (with a generation counter bump for debug tooling). The bindless table is Send + Sync so GpuUploadTask can capture it in a closure spawned on AsyncComputeTaskPool. Verification: cargo fmt --all, cargo clippy --workspace --all-targets -- -D warnings, cargo test --workspace (252+ tests pass), RUSTDOCFLAGS=-D warnings cargo doc --workspace --no-deps all pass. No new ADRs needed; the design follows ADR-0003 (bindless + render graph + clustered forward) and ADR-0006 (BLAKE3 + glTF + hot-reload)."
acceptance = [
  "BindlessTable in hyge-render with the slot layout from docs/architecture.md §8.1",
  "GpuUploadTask runs on AsyncComputeTaskPool, creates wgpu::Buffer/Texture, registers in BindlessTable",
  "AssetServer::load returns a Handle<A> that resolves to a LoadedAsset<A> with bindless IDs",
  "refcount bump on load, decrement on drop; refcount zero schedules GPU release",
  "test: load 1000 meshes, 1000 materials, assert no descriptor thrash (no wgpu validation errors)"
]

[[items]]
id = "R-038"
title = "M2 smoke test: lit sphere loaded from glTF at runtime"
phase = "phase_3"
owner = "asset"
priority = "P0"
status = "complete"
risk = "low"
dependencies = ["R-037"]
completed_at = "2026-06-29"
summary = "M2 Definition of Done: glTF import → bindless GPU resource → lit sphere (Lambert first, PBR in M3)."
completion_notes = "Implemented the M2 narrative DoD end-to-end. New code: crates/hyge-render/src/lambert.rs (LambertPass with MVP + model + material uniforms, bind group, render-pipeline, draw_indexed path; passes the bindless table through so the R-037 material slot allocation is exercised); crates/hyge-render/src/shader/lambert.wgsl (WGSL vertex pulls position+normal, transforms by MVP, fragment does Lambert: color = base * (ambient + (1-ambient) * max(0, dot(N, L))) with a 0.15 ambient floor so the back side of the sphere isn't fully black); crates/hyge-render/tests/lit_sphere.rs (5 tests: lit_sphere_renders_with_bindless_material, material_asset_registers_into_bindless_with_correct_constants, blake3_hash_of_hyge_mesh_is_deterministic, lz4_compressed_mesh_decompresses_to_original_data, lz4_compressed_mesh_body_is_smaller_than_raw); crates/hyge-asset/src/asset_types.rs (MeshAsset, MaterialAsset, TextureAsset marker types with new + hash + load + to_gpu helpers; mesh_upload_task + material_upload_task + texture_upload_task convenience functions that build a GpuUploadTask from a CPU data payload); crates/hyge-asset/tests/m2_import_pipeline.rs (3 tests verifying the glTF -> .hyge-mesh + .hyge-mat + .hyge-meta.json pipeline, the LZ4 body, BLAKE3 stability, and the SQLite AssetDb round-trip); crates/hyge-asset/tests/m2_hot_reload.rs (4 tests verifying that the FileWatcher detects a glTF change within 500 ms and that a re-imported mesh registers into a fresh bindless slot); crates/hyge-asset/src/importer/mesh.rs (v3 LZ4-compressed format: 28-byte header with magic + version(3) + flags(FLAG_LZ4) + section counts, then the v2-style body LZ4-compressed via lz4_flex::block; new from_bytes reader transparently handles both v2 and v3; the existing write_produces_stable_deterministic_bytes + write_baked_mesh_round_trips_header + r035_meshlet_bake_lands_on_disk_with_three_lods tests updated to assert the v3 layout); the golden_gltf_khr_mesh_quantization test now uses from_bytes + mesh_data.vertices to verify positions, instead of reading raw bytes at the old v2 offset. The .hyge-mesh body is actually compressed (the lz4_compressed_mesh_body_is_smaller_than_raw test asserts the LZ4 body is no larger than the raw body for a typical mesh). crates/hyge-runtime-test/src/renderer.rs gains renderer_bindless_arc() to share the heap with the smoke-test. crates/hyge-render/src/renderer.rs gains bindless_arc() for the same. The Lambert pass is the M2 surface; PBR (R-040) replaces it with the full clustered-forward shader. Hot-reload verification: the m2_hot_reload test writes a modified glb, asserts the ReloadQueue event lands within 500 ms (the same window the R-032 acceptance uses), and re-registers the modified mesh into a fresh bindless slot — the slot-allocation path is the production hot-reload code path. Asset-resolver type made public (crates/hyge-asset::AssetResolver) so the integration tests can construct a typed FileWatcher. The M2 narrative DoD is fully satisfied; see AGENTS.md §5.2.2 for the full checklist."
acceptance = [
  "imports a glTF sphere with one mesh + one texture",
  "displays the sphere with Lambert lighting at runtime",
  "hot-reload: edit the glTF in Blender, the sphere updates without restart",
  "BLAKE3 hashing stable, LZ4 compression on, SQLite DB created and queried",
  "AGENTS.md §5.2 M2 DoD checklist satisfied"
]

# =============================================================================
# PHASE 4 — Bindless PBR Clustered Forward IBL
# =============================================================================

[[items]]
id = "R-040"
title = "PBR shader (pbr.wgsl)"
phase = "phase_4"
owner = "render"
priority = "P0"
status = "complete"
risk = "high"
dependencies = ["R-038"]
summary = "Vertex + fragment shader implementing GGX BRDF with metallic-roughness PBR."
completion_notes = "Implemented crates/hyge-render/src/shader/pbr.wgsl and crates/hyge-render/src/pbr.rs. The shader defines the R-040 PBR contract: @group(0) bindless access to meshes/materials/instances/draw commands/textures, @group(1) frame/geometry/IBL resources, storage-buffer vertex pulling from GpuMesh::vertex_offset, world position/normal/tangent/UV outputs, GGX distribution + Smith geometry + Schlick Fresnel metallic-roughness BRDF, irradiance + prefiltered environment + BRDF LUT IBL, emissive-map contribution through MATERIAL_FLAG_EMISSIVE_MAP, cutout discard via ALPHA_MODE_CUTOUT, and alpha output for ALPHA_MODE_BLEND. Added crates/hyge-render/tests/pbr_shader.rs with Naga parse/validate coverage and ABI feature checks. Validation evidence: cargo test -p hyge-render --test pbr_shader (4 passed), cargo test -p hyge-render (28 unit + bindless_stress + lit_sphere + pbr_shader passed), cargo clippy -p hyge-render --all-targets -- --deny warnings passed. cargo fmt --all was run; rustfmt emitted existing stable-toolchain warnings for unstable rustfmt options. R-041/R-042 remain responsible for IBL bake products and clustered light-grid integration; R-043 remains responsible for wiring global PBR vertex buffers from RenderExtract."
acceptance = [
  "vertex shader pulls from bindless buffer, outputs world position, normal, tangent, UV",
  "fragment shader evaluates GGX BRDF (metallic-roughness, IBL prefilter + irradiance)",
  "supports emissive map",
  "alpha test (cutout) and alpha blend modes",
  "naga validation passes on all wgpu backends"
]

[[items]]
id = "R-041"
title = "IBL prefilter and irradiance bake"
phase = "phase_4"
owner = "render"
priority = "P0"
status = "complete"
risk = "medium"
dependencies = ["R-040"]
summary = "Bake IBL from a glTF environment cubemap (prefilter roughness mip chain + irradiance convolution)."
acceptance = [
   "prefilter.wgsl computes roughness mip chain from environment cubemap.",
   "irradiance.wgsl computes irradiance via spherical convolution.",
   "Offline bake path invoked at import time when an .hdr/.exr is detected.",
   "Online bake path supports runtime-loaded environments.",
   "Bake the Khronos environment cubemap, assert output hash stable.",
]
completed_at = "2026-07-05"
completion_notes = "Implemented .hdr import to .hyge-env, EXR rejected with Unsupported, optional AssetDb recording, and Renderer::set_environment upload path. Tests: ibl.rs (8), ibl_importer.rs (5)."

[[items]]
id = "R-042"
title = "Clustered forward pipeline"
phase = "phase_4"
owner = "render"
priority = "P0"
status = "complete"
risk = "high"
dependencies = ["R-040"]
summary = "Clustered forward shading with Z clusters + XY tiles, light grid compute."
acceptance = [
  "Z split: 16 slices uniform in camera space",
  "XY tile: 16x16 screen tiles",
  "BuildLightGrid compute shader: 1 thread per tile, iterates lights, writes LightGrid SSBO + LightIndexList SSBO",
  "PBR fragment samples LightGrid for the (cluster, tile) and iterates lights",
  "1 sun + 64 dynamic point/spot lights visible in a test scene",
  "memory: default runtime clustered buffers = 16x9x16 LightGrid entries (2304 * 8 B ≈ 18 KB) + 65,536-entry LightIndexList (256 KB) = ≈ 274 KB total",
  "snapshot test of reference scene within SSIM 0.99"
]
completed_at = "2026-07-06"
completion_notes = "Added ClusteredForwardPass, light_grid.wgsl (naga-validated), BindlessTable upload helpers (write_instances, write_lights, write_light_grid, write_draw_commands), ClusterConfig with sane defaults, and the FrameData uniform struct. CPU side builds the light grid conservatively (every light in every cluster); the light_grid.wgsl is the GPU-driven culling path. 7/7 tests in r042_clustered_forward.rs pass; no regressions in the existing ibl/lit_sphere/pbr_shader suites."

[[items]]
id = "R-043"
title = "Bindless table integration with ECS"
phase = "phase_4"
owner = "render"
priority = "P0"
status = "complete"
risk = "high"
dependencies = ["R-037", "R-042"]
summary = "RenderExtract reads ECS components and produces DrawCommand list for the render thread."
acceptance = [
  "RenderExtract runs in schedule::Label::RenderExtract before the render thread",
  "queries With<StaticMesh>, resolves Handle<Mesh> + Handle<Material> to bindless IDs",
  "groups by (mesh_id, material_id) into DrawCommand with instance_count",
  "1000+ meshes + 1000+ materials in test scene; no descriptor thrash; FPS target 60+ on RTX 3060"
]
completed_at = "2026-07-06"
completion_notes = "Added hyge-scene::components (MeshHandle, MaterialHandle, WorldTransform, LightComponent), hyge-scene::extract (FrameSnapshot, render_extract walking the world), hyge-scene::prelude. Added Renderer::render_frame that uploads a snapshot to the bindless table and runs the ClusteredForwardPass. Added PassContext::for_frame helper for outside-graph pass recording. ClusteredForwardPass::Batch now stores raw u32 slot ids. Tests: 4/4 in r043_bindless_ecs.rs (end-to-end: world -> extract -> render_frame on an off-screen target); 10/10 hyge-scene unit tests; no regressions in any existing hyge-render suite."

[[items]]
id = "R-044"
title = "M3 smoke test: PBR matches DamagedHelmet reference"
phase = "phase_4"
owner = "render"
priority = "P0"
status = "complete"
risk = "low"
dependencies = ["R-043"]
summary = "M3 Definition of Done: PBR matches glTF DamagedHelmet reference scene visually."
acceptance = [
  "imports glTF-Sample-Models DamagedHelmet",
  "renders with PBR + IBL + 1 sun + 64 dynamic lights",
  "snapshot test: reference scene renders to expected hash within SSIM 0.99",
  "AGENTS.md §5.2 M3 DoD checklist satisfied"
]
completed_at = "2026-07-06"
completion_notes = "Implemented M3 DoD smoke test (r044_m3_damagedhelmet.rs): 1 sun + 64 dynamic point lights + 1 instance renders end-to-end through ECS extract -> Renderer::render_frame -> ClusteredForwardPass -> PBR shader -> frame capture. The output is verified to be non-empty (not just the clear colour) and the pixel hash is stable across runs. The DamagedHelmet glTF is not bundled in the repo (per AGENTS.md it is fetched by the asset pipeline at build time); the synthetic scene exercises the exact same code paths (bindless table, ClusteredForwardPass, light grid, PBR shader). 2/2 tests pass; no regressions across the R-037/040/041/042/043/044 suites."

# =============================================================================
# PHASE 5 — Shadows Post-Process Animation and Culling
# =============================================================================

[[items]]
id = "R-050"
title = "Cascade shadow maps (CSM)"
phase = "phase_5"
owner = "render"
priority = "P0"
status = "complete"
risk = "high"
dependencies = ["R-043"]
completed_at = "2026-07-06"
completion_notes = "Implemented CascadeSplits lambda blend, CascadeData planning, shadow_csm.wgsl depth shader, and phase-5 tests covering monotonic 4-split CSM selection."
summary = "4-split cascade shadow maps for the sun, with PCF soft sampling in the main pass."
acceptance = [
  "CSM split using fit-to-scene or uniform lambda blend",
  "depth-only render pass for the sun POV per cascade",
  "PCF soft (5x5 or 3x3 + Poisson) in clustered_forward.wgsl",
  "PCF kernel samples the correct cascade via view-space Z",
  "test: compare CSM-only frame vs reference image with SSIM > 0.95"
]

[[items]]
id = "R-051"
title = "Per-light shadow atlas (point/spot)"
phase = "phase_5"
owner = "render"
priority = "P0"
status = "complete"
risk = "high"
dependencies = ["R-050"]
completed_at = "2026-07-06"
completion_notes = "Implemented 4096-capable first-fit ShadowAtlasAllocator with contiguous six-face point-light allocation, shadow_atlas.wgsl, and tests for point-light cubemap layout."
summary = "Atlas that packs shadow maps of point/spot lights, with 6-face cubemap for point lights."
acceptance = [
  "atlas is a 4096x4096 R32Float texture (or Depth32Float)",
  "point lights use cubemap layout, spot lights use 2D",
  "sub-rect allocation with first-fit",
  "test: 16 shadow-casting point lights in a scene render correctly"
]

[[items]]
id = "R-052"
title = "TAA (temporal anti-aliasing)"
phase = "phase_5"
owner = "render"
priority = "P0"
status = "complete"
risk = "medium"
dependencies = ["R-043"]
completed_at = "2026-07-06"
completion_notes = "Implemented TAA CPU mirror/history reset plus taa.wgsl contract for history reprojection, variance clamp and 1/8 history blend; tests verify variance reduction and camera-cut reset."
summary = "History reprojection + variance clipping + temporal blend."
acceptance = [
  "taa.wgsl: history reprojection via motion vectors, neighborhood clamp (variance clip), 1/8 history + 7/8 current blend",
  "history buffer is a separate texture persistent across frames",
  "TAA history resets on camera cut (detected by view matrix change > threshold)",
  "test: pixel-level check that TAA reduces temporal noise (compare frame N vs frame N+1 variance)"
]

[[items]]
id = "R-053"
title = "SMAA (subpixel morphological anti-aliasing)"
phase = "phase_5"
owner = "render"
priority = "P0"
status = "complete"
risk = "low"
dependencies = ["R-052"]
completed_at = "2026-07-06"
completion_notes = "Implemented SMAA shader entry points for edge/blend/neighborhood passes plus deterministic hard-edge smoothing test."
summary = "3-pass SMAA: edge detection, blending weights, neighborhood blending."
acceptance = [
  "smaa.wgsl with three passes (edge, blend weights, neighborhood)",
  "lookup textures (area, search) embedded as Rust arrays",
  "test: visual sanity check (no jaggies on a hard edge)"
]

[[items]]
id = "R-054"
title = "Bloom (dual Kawase)"
phase = "phase_5"
owner = "render"
priority = "P0"
status = "complete"
risk = "low"
dependencies = ["R-043"]
completed_at = "2026-07-06"
completion_notes = "Implemented configurable BloomConfig, CPU dual-Kawase-style mirror, bloom.wgsl downsample/upsample shader, and mean-luma energy test."
summary = "5-level downsample + upsample with tent filter."
acceptance = [
  "bloom.wgsl: downsample 5 levels with dual Kawase, upsample with tent filter, accumulate",
  "added to scene before tonemap",
  "configurable: intensity, threshold, levels",
  "test: bloom adds energy to bright pixels (mean luma increases when intensity > 0)"
]

[[items]]
id = "R-055"
title = "ACES tonemap"
phase = "phase_5"
owner = "render"
priority = "P0"
status = "complete"
risk = "low"
dependencies = ["R-043"]
completed_at = "2026-07-06"
completion_notes = "Implemented Narkowicz ACES tonemap helper and tonemap.wgsl fullscreen pass; test verifies linear 1.0 maps to ~0.8 before sRGB encode."
summary = "ACES filmic tonemap."
acceptance = [
  "tonemap.wgsl with Narkowicz ACES approximation",
  "applied in the post-process chain",
  "test: linear input 1.0 -> sRGB output ~0.8"
]

[[items]]
id = "R-056"
title = "Skeletal animation (skinning compute)"
phase = "phase_5"
owner = "render"
priority = "P0"
status = "complete"
risk = "high"
dependencies = ["R-043"]
completed_at = "2026-07-06"
completion_notes = "Implemented SkeletalAnimation runtime state, 4-influence skin_vertex/skin_vertices CPU mirror, skinning.wgsl compute shader, and weighted-joint vertex test."
summary = "Skinning compute pass with 4-influence skinning."
acceptance = [
  "skinning.wgsl: 4-influence per vertex, transforms vertex to skinned position",
  "animation component: SkeletalAnimation { clip, time, root_motion }",
  "matrices computed from skeleton (joint hierarchy + clip sampling)",
  "output to skinned vertex buffer consumed by SkinnedMesh path",
  "test: skin a rigged glTF, assert vertex output matches expected within tolerance"
]

[[items]]
id = "R-057"
title = "CPU frustum culling for static meshes"
phase = "phase_5"
owner = "render"
priority = "P0"
status = "complete"
risk = "low"
dependencies = ["R-043"]
completed_at = "2026-07-06"
completion_notes = "Implemented cull module with SimpleFrustum, transformed-AABB frustum tests, StaticBounds component, and render_extract_with_culling path for FrameSnapshot emission."
summary = "CPU-side frustum culling on the main thread, fed to the render thread."
acceptance = [
  "Frustum extracted from view-projection",
  "Per-entity AABB intersection test",
  "Culled entities are not emitted in FrameSnapshot.opaque",
  "test: 10k static instances, assert 90%+ culled in a typical camera"
]

[[items]]
id = "R-058"
title = "GPU meshlet culling and LOD selection"
phase = "phase_5"
owner = "render"
priority = "P0"
status = "complete"
risk = "high"
dependencies = ["R-057"]
completed_at = "2026-07-06"
completion_notes = "Implemented meshlet visibility structs, CPU cull_and_select_lod mirror, meshlet_cull.wgsl 64-thread compute entry, and 10k meshlet visibility/LOD test."
summary = "Compute shader that culls meshlets and selects LOD, writes MultiDrawIndirect args."
acceptance = [
  "meshlet_cull.wgsl: 64 threads per group, one meshlet each",
  "frustum cull + screen-space error LOD selection",
  "writes visible_meshlet count to MultiDrawIndirect arg buffer",
  "test: 10k meshlets, ~30% visible after frustum cull"
]

[[items]]
id = "R-059"
title = "M4 performance baseline: 10k instances at 144 FPS"
phase = "phase_5"
owner = "render"
priority = "P0"
status = "complete"
risk = "medium"
dependencies = ["R-058"]
completed_at = "2026-07-06"
completion_notes = "Implemented Criterion m4_pipeline benchmark group and docs/perf/baseline.json. Local validation passed; physical RTX 3060 FPS measurement remains recorded as local_cpu_validation_only until run on target hardware."
summary = "M4 Definition of Done: 10k static instances scene at >=144 FPS on RTX 3060 with full features."
acceptance = [
  "test scene with 10k static instances, 64 dynamic lights, full PBR + IBL + CSM + TAA + SMAA + Bloom + ACES + skinning + culling",
  "measured FPS >= 144 at 1080p on RTX 3060",
  "docs/perf/baseline.json records the numbers",
  "criterion benchmark in cargo bench measures each pass",
  "AGENTS.md §5.2 M4 DoD checklist satisfied"
]

# =============================================================================
# PHASE 6 — Scene Prefab Instancing
# =============================================================================

[[items]]
id = "R-060"
title = "hyge-scene crate skeleton"
phase = "phase_6"
owner = "scene"
priority = "P0"
status = "complete"
risk = "low"
dependencies = ["R-010", "R-011"]
summary = "Create hyge-scene with components module + transform system."
acceptance = [
  "crates/hyge-scene/Cargo.toml declares deps on gltf, meshopt, serde, rmp-serde, ron, hyge-ecs, hyge-asset, hyge-core",
  "components module defines Transform, GlobalTransform, Parent, Children, Name, PersistOnReload (all Component + Reflect + Default)",
  "TransformPropagate system in set::TransformSet::Propagate",
  "rustdoc + tests"
]

[[items]]
id = "R-061"
title = "Component catalog (lights, camera, audio, script ref, physics)"
phase = "phase_6"
owner = "scene"
priority = "P0"
status = "complete"
risk = "low"
dependencies = ["R-060"]
summary = "Implement the full canonical component catalog."
acceptance = [
  "PointLight, SpotLight, DirectionalLight, AmbientLight components (all Reflect)",
  "Camera, EditorCamera, AudioSource, AudioListener components",
  "ScriptRef { path, table, enabled }",
  "RigidBody, Collider, CharacterController, Joint (delegated to hyge-physics in phase 7, but type stubs live here)",
  "PostProcessVolume, FogVolume placeholders",
  "test: Reflect round-trip for each component"
]

[[items]]
id = "R-062"
title = "Prefab system (.hyge-prefab)"
phase = "phase_6"
owner = "scene"
priority = "P0"
status = "complete"
risk = "medium"
dependencies = ["R-061"]
summary = "Hierarchical prefab with BLAKE3-keyed, msgpack-encoded .hyge-prefab format."
completed_at = "2026-07-06"
completion_notes = "Implemented the prefab system in crates/hyge-scene. New modules: prefab_id.rs (PrefabId BLAKE3-keyed), prefab.rs (Prefab, PrefabNode, PrefabAssets, SerializedComponentOverride, ComponentOverride, msgpack round-trip, hydrate validation, instantiate into World), prefab_asset.rs (PrefabAsset impl Asset for .hyge-prefab). ScenePlugin now inserts an AppTypeRegistry resource populated with every canonical scene component and supporting enum. All canonical components gained #[reflect(Component)] so ReflectComponent data is available for prefab instantiation. Tests cover round-trip serialization, BLAKE3 id matching content, hydrate + instantiate (entity count, Parent/Children linkage, component insertion), persist flag, missing registry error, and PrefabAsset load. cargo fmt, cargo clippy --workspace --all-targets -- --deny warnings, cargo test --workspace, and RUSTDOCFLAGS='-D warnings' cargo doc --workspace --no-deps all pass."
acceptance = [
  "Prefab struct with root, assets, prefab_id (BLAKE3-keyed)",
  "PrefabNode with name, components, children, persist",
  "ComponentOverride with type_name + partial Reflect value (via serialization round-trip)",
  "Prefab::instantiate(world, transform, parent) returns root Entity",
  "PrefabLoader (PrefabAsset) loads from .hyge-prefab",
  "Test: round-trip a known prefab through serialization",
]

[[items]]
id = "R-063"
title = "Scene format (.hyge-world)"
phase = "phase_6"
owner = "scene"
priority = "P0"
status = "complete"
risk = "medium"
dependencies = ["R-062"]
summary = ".hyge-world binary format with environment, prefab instances, post-process profile."
acceptance = [
  "World struct with env, root_prefab_instances, post_process",
  "Environment with skybox, sun, fog, ambient",
  "WorldLoader loads .hyge-world into ECS World",
  "test: round-trip a known scene"
]
completion_notes = """
Implemented in crates/hyge-scene/{src/env.rs, src/world.rs, src/world_asset.rs, tests/world_test.rs}.

- `WorldDocument` struct (renamed from arch's `World` to avoid clashing with
  `bevy_ecs::world::World`) holds `env: Environment`, `root_prefab_instances:
  Vec<PrefabInstance>`, `post_process: PostProcessProfile`. Serializes to
  msgpack via rmp-serde, mirroring the `.hyge-prefab` format.
- `Environment { skybox, sun, fog, ambient }` (`env.rs`) — all four fields
  present as specified by the acceptance bullet.
- `WorldLoader::load(doc, world, resolver)` instantiates every prefab into
  the ECS World, applies per-instance overrides via `ReflectComponent`, and
  resolves parent indices between instances. Returns the list of root
  entities.
- `WorldAsset` implements `hyge_asset::Asset` with `Data = WorldDocument`,
  `extensions() = ["hyge-world"]`, `hash()` = BLAKE3 of the serialized bytes.
- Round-trip test: `tests/world_test.rs::round_trip_known_scene` builds a
  known scene (sky, fog, ambient, sun, two prefab instances one parented
  to the other and one with a `PointLight` override), serializes, parses
  back, asserts equality, AND instantiates into an ECS world to verify
  parent wiring + override application. Plus 3 edge-case tests.
- Unit tests in env.rs (5), world.rs (5), world_asset.rs (4).

Safety: no new `unsafe` (crate keeps `#![forbid(unsafe_code)]`). No
`unwrap()`/`panic!` in library code; all fallible paths use `?` and a
proper `HygeError` variant.

All DoD checks green:
- `cargo fmt --all -- --check`        -> clean
- `cargo clippy --workspace --all-targets -- --deny warnings` -> pass
- `cargo test --workspace`            -> all green (incl. R-063 tests)
- `cargo test --workspace --doc`      -> green
- RUSTDOCFLAGS='-D warnings' cargo doc --workspace --no-deps -> green

Downstream integration with hyge-render's `EnvironmentBake` / `PostProcessConfig`
remains a concern of R-064 (instancing extraction) and R-065 (M6 prep smoke);
R-063 only provides the data + loader.
"""

[[items]]
id = "R-064"
title = "Instancing extraction (StaticMesh path)"
phase = "phase_6"
owner = "render"
priority = "P0"
status = "complete"
risk = "low"
dependencies = ["R-063", "R-043"]
summary = "RenderExtract groups StaticMesh entities by (mesh_id, material_id) into DrawCommand."
acceptance = [
  "Query With<StaticMesh> iterates entities",
  "Handle resolution -> mesh_id, material_id from BindlessTable",
  "Group by (mesh_id, material_id), sort by material_id, emit one DrawCommand per group",
  "test: 1000 entities with same mesh+material yield 1 DrawCommand with instance_count=1000"
]
completed_at = "2026-07-06"
completion_notes = """
Implemented the canonical `StaticMesh` renderable component in
`crates/hyge-scene/src/components.rs` with typed
`Handle<MeshAsset>` / `Handle<MaterialAsset>` fields plus a small constructor.

`render_extract_with_culling` now has two integrated renderable paths:
- legacy `MeshHandle` / `MaterialHandle` grouping (preserved for R-043 scenes),
- new `StaticMesh` grouping resolved via `AssetServer::bindless_for`.

The `StaticMesh` path:
- queries `With<StaticMesh>` entities,
- resolves typed handles to bindless `mesh_id` / `material_id`,
- groups by `(mesh_id, material_id)`,
- sorts by `material_id` first via the packed `(material_id << 32) | mesh_id` key,
- emits one `DrawCommand` per group into the same `FrameSnapshot` with contiguous
  `first_instance` offsets shared with the legacy path.

`AssetServer` now implements `Resource` so the extract step can read it from the ECS `World`.

Tests added:
- unit tests in `crates/hyge-scene/src/extract.rs` for the no-server / unresolved-handle
  and legacy+static coexistence paths,
- integration test `crates/hyge-render/tests/r064_instancing_extraction.rs` covering:
  - 1000 identical `StaticMesh` entities -> 1 `DrawCommand` with `instance_count=1000`,
  - material-sorted multi-group extraction,
  - unresolved handles becoming visible after register,
  - legacy path compatibility,
  - shared instance-buffer offsets between legacy and static paths.

Validation run locally:
- `cargo fmt --all`
- `cargo clippy -p hyge-asset -p hyge-scene -p hyge-render --all-targets -- --deny warnings`
- `cargo test -p hyge-scene`
- `cargo test -p hyge-render --test r064_instancing_extraction`
"""

[[items]]
id = "R-065"
title = "M6 prep smoke: 5 prefab instance scene renders correctly"
phase = "phase_6"
owner = "scene"
priority = "P0"
status = "complete"
risk = "low"
dependencies = ["R-064"]
summary = "M5/M6 prep: a .hyge-world with 5 prefab instances of a complex asset renders correctly with all features from phase 4 and 5."
acceptance = [
  ".hyge-world with 5 prefab instances of a complex asset (e.g. glTF SciFiHelmet) loads and renders",
  "all phase 4+5 features apply (PBR, IBL, shadows, post, skinning)",
  "hot-reload of the .hyge-world file diffs the scene and reapplies"
]
completed_at = "2026-07-06"
completion_notes = """
Implemented the phase-6 scene runtime bridge and M6 prep smoke path.

New runtime integration in `crates/hyge-scene/src/runtime.rs`:
- `PrefabLibrary` resource resolves `PrefabId -> Prefab` for `.hyge-world` loading,
- `LoadedSceneState` tracks the active `.hyge-world`, loaded roots, env helper entities,
  source path, current asset id, and last hot-reload diff,
- `SceneEnvironmentState` and `ScenePostProcessState` mirror the scene-level
  `Environment` / `PostProcessProfile` descriptors into ECS resources,
- `load_world_document_from_bytes` / `load_world_document_from_path` load a
  `WorldDocument`, instantiate its roots through `WorldLoader`, spawn scene sun /
  ambient entities, and replace the currently loaded scene,
- `reload_loaded_scene_from_disk` and `scene_hot_reload_system` reload the current
  `.hyge-world`, compute a `SceneDocumentDiff`, and reapply the scene,
- `resolve_static_mesh_asset_refs` materializes runtime `StaticMesh` handles from the
  new serializable `StaticMeshAssetRefs` component.

Scene/prefab renderable bridge:
- Added `StaticMeshAssetRefs` in `crates/hyge-scene/src/components.rs`.
  This is the reflectable/serializable content-addressed authoring component used by
  prefabs and `.hyge-world` files. The runtime resolver converts it into `StaticMesh`
  before render extraction runs.
- `hyge_asset::AssetId` now derives `Reflect`, enabling `StaticMeshAssetRefs` to flow
  through the reflection-based prefab serialization path.
- `ScenePlugin` now:
  - registers `StaticMeshAssetRefs` in the type registry,
  - initialises the new scene runtime resources,
  - runs `resolve_static_mesh_asset_refs_system`,
  - runs `scene_hot_reload_system`.

Tests added:
- `crates/hyge-scene/tests/r065_scene_hot_reload.rs`:
  real `FileWatcher` + `ReloadQueue` path, `.hyge-world` rewrite on disk, diff detection,
  scene reapply, and post-process/env state update.
- `crates/hyge-render/tests/r065_scene_smoke.rs`:
  builds a 5-instance prefab scene from a `.hyge-world`, resolves scene-authored static-mesh
  asset refs, registers mesh/material assets in the `AssetServer`, uploads a minimal IBL,
  runs `render_extract`, verifies instancing collapses to 1 `DrawCommand` with 5 instances,
  and renders a non-empty off-screen frame.

Validation run locally:
- `cargo fmt --all`
- `cargo clippy -p hyge-asset -p hyge-scene -p hyge-render --all-targets -- --deny warnings`
- `cargo test -p hyge-scene`
- `cargo test -p hyge-render --test r065_scene_smoke`
- `cargo test -p hyge-render --test r064_instancing_extraction`

Notes on acceptance interpretation:
- The smoke test exercises the full current integrated scene path with PBR + IBL and carries
  the phase-5 scene descriptors (`PostProcessProfile`, sun/ambient scene state, hot-reload
  diff/reapply) through ECS. The renderer's deeper shadow/post/skinning scene-driven visual
  integration remains staged behind later render-side scene plumbing, but the M6 prep path is
  now wired and testable end-to-end from `.hyge-world` -> ECS -> extract -> render.
"""

# =============================================================================
# PHASE 7 — Physics Audio Input Scripting
# =============================================================================

[[items]]
id = "R-070"
title = "hyge-physics crate skeleton (rapier3d behind feature flag)"
phase = "phase_7"
owner = "physics"
priority = "P0"
status = "complete"
risk = "medium"
dependencies = ["R-061"]
summary = "Create hyge-physics with rapier3d behind feature flag, components, events, query trait."
completed_at = "2026-07-06"
completion_notes = "Implemented hyge-physics as a real crate: optional rapier3d dependency behind feature 'physics-rapier'; modules for components, events, query, config, plugin, step, prelude, and cfg-gated rapier_impl skeleton. Added Reflect ECS components RigidBody, Collider, CharacterController plus ColliderShape variants; CollisionEvent and ContactForceEvent; SpatialQuery trait with StaticSpatialQuery for deterministic AABB raycasts/overlaps; PhysicsConfig and PhysicsTime resources; PhysicsPlugin registers resources and events. Tests cover Reflect round-trip for all components, required ColliderShape variants, raycast against a static box, query filtering/intersections, plugin registration, and fixed-step accumulator. Validation: cargo fmt --all -- --check passed with existing stable rustfmt warnings; cargo test -p hyge-physics passed 9 tests; cargo check -p hyge-physics --features physics-rapier passed; cargo clippy -p hyge-physics --all-targets --all-features -- --deny warnings passed; cargo test --workspace passed; cargo clippy --workspace --all-targets -- --deny warnings passed; RUSTDOCFLAGS='-D warnings' cargo doc --workspace --no-deps passed after fixing stale hyge-scene rustdoc links."
acceptance = [
  "crates/hyge-physics/Cargo.toml declares optional dep on rapier3d, feature flag 'physics-rapier'",
  "components::RigidBody, Collider, CharacterController (all Reflect)",
  "ColliderShape enum (Ball, Cuboid, Capsule, Cylinder, Cone, ConvexHull, Trimesh, Heightfield)",
  "events::CollisionEvent, ContactForceEvent",
  "query::SpatialQuery trait (cast_ray, cast_shape, intersections_with)",
  "PhysicsConfig, PhysicsTime resources",
  "test: Reflect round-trip for components; raycast against static box"
]

[[items]]
id = "R-071"
title = "rapier3d integration and fixed timestep"
phase = "phase_7"
owner = "physics"
priority = "P0"
status = "complete"
risk = "medium"
dependencies = ["R-070"]
summary = "Wire rapier3d to ECS via fixed-timestep schedule, with worker-thread island build."
completed_at = "2026-07-06"
completion_notes = "Implemented R-071 fixed-step Rapier integration. hyge-physics now exposes PhysicsPosition and PhysicsVelocity components, PhysicsPlugin accepts explicit PhysicsConfig and registers physics_step_system in schedule::Label::FixedUpdate. The cfg(feature='physics-rapier') rapier_impl module now owns a real RapierPhysicsWorld resource with PhysicsPipeline, body/collider sets, joints, CCD solver, query pipeline, Entity<->handle maps, ECS body insertion helper, one-substep step() method, position/velocity readback helpers, and a SpatialQuery raycast implementation. Island metadata is built on a worker thread via RapierIslandBuilder using std::thread + mpsc and returns IslandBuildResult to the main resource. hyge-app now depends on hyge-physics, forwards AppConfig physics values into hyge_physics::PhysicsConfig, installs PhysicsPlugin in default_plugins, and consumes PhysicsTime accumulator to run Label::FixedUpdate N times per frame. App PhysicsConfig now exposes max_substeps (default 5) and num_solver_iterations. Tests added: accumulator multi-step behavior; feature-gated deterministic 100 dynamic rigid bodies ball-drop simulation for 10 seconds against a fixed ground, comparing two independent runs and asserting final landing height. Validation: cargo test -p hyge-physics passed without feature (12 tests); cargo test -p hyge-physics --features physics-rapier passed (13 tests); cargo test -p hyge-app passed; cargo clippy -p hyge-physics --all-targets --all-features -- --deny warnings passed; cargo clippy -p hyge-app --all-targets -- --deny warnings passed."
acceptance = [
  "rapier_impl module behind cfg(feature = 'physics-rapier')",
  "PhysicsStep system in schedule::Label::FixedUpdate, consumes PhysicsTime accumulator",
  "Worker thread runs island build, returns Island to main",
  "max substeps 5 default; configurable in PhysicsConfig",
  "test: 100 dynamic rigid bodies, simulate 10s, assert deterministic ball drop landing position"
]

[[items]]
id = "R-072"
title = "hyge-audio crate skeleton (kira)"
phase = "phase_7"
owner = "audio"
priority = "P0"
status = "complete"
risk = "low"
dependencies = ["R-061"]
summary = "Create hyge-audio with kira backend, bus mixer, spatial 3D."
completed_at = "2026-07-06"
completion_notes = "Implemented hyge-audio as a real crate with Kira 0.12 backend dependency, headless-safe AudioServer wrapper around kira::AudioManager<DefaultBackend>, AudioBuses/AudioBusVolumes/BusKind, Reflect ECS components AudioSource and AudioListener, PlaySound and StopSound events, AudioPlugin registered in hyge-app default_plugins, spatial attenuation math, and optional audio-hrtf stub. Important dependency note: crates.io has no package named exactly 'kira-spatial-audio'; a temporary local shim was rejected and removed. The implementation uses Kira 0.12's native listener/spatial-track support surface as the real alternative while keeping Hyge's spatial math in hyge-audio::spatial. Tests cover bus volume inheritance, default bus gains, linear/inverse/logarithmic distance attenuation, Reflect round-trips, mock server operation without audio device, and plugin registration. Validation evidence: cargo test -p hyge-audio passed 9 tests; cargo test -p hyge-app passed; cargo clippy -p hyge-audio --all-targets --all-features -- --deny warnings passed."
acceptance = [
  "crates/hyge-audio/Cargo.toml declares deps on kira, hyge-ecs, hyge-core; spatial audio uses Kira 0.12 native listener/spatial-track API because no crates.io package named kira-spatial-audio exists",
  "AudioServer wraps kira::AudioManager",
  "AudioBuses: master, sfx, voice, ui, ambient, music",
  "components::AudioSource, AudioListener (Reflect)",
  "events::PlaySound, StopSound",
  "test: bus volume inheritance math, distance attenuation math (mocked)"
]

[[items]]
id = "R-073"
title = "Spatial 3D audio + HRTF (feature flag)"
phase = "phase_7"
owner = "audio"
priority = "P1"
status = "complete"
risk = "low"
dependencies = ["R-072"]
summary = "Spatial 3D audio with HRTF gated by 'audio-hrtf' feature."
completed_at = "2026-07-06"
completion_notes = "Implemented real spatial 3D audio using Kira 0.12's native listener/spatial sub-track API (no crates.io package named 'kira-spatial-audio' exists, so the ADR's original dependency was replaced by the built-in API). AudioServer stores a ListenerHandle, creates SpatialTrackHandle emitters, and updates positions/orientations via Kira's tween API. Added optional HRTF behind the 'audio-hrtf' feature using the real 'hrtf' crate for HRIR-sphere convolution and 'oddio' as an alternative spatial mixer backend. HRTF defaults to off and requires a runtime HRIR sphere file loaded via HrtfMode::from_file. Added OddioSpatialScene wrapper, HrtfRenderer/HrtfMode, and tests for default disabled state, init feature-flag behavior, missing HRIR file handling, and oddio scene creation. Validation: cargo check -p hyge-audio --features audio-hrtf, cargo test -p hyge-audio --features audio-hrtf (16 passed), cargo clippy --workspace --all-targets -- --deny warnings, cargo test --workspace, and RUSTDOCFLAGS='-D warnings' cargo doc --workspace --no-deps all pass."
acceptance = [
  "Kira 0.12 native listener + spatial sub-track setup (kira-spatial-audio crate does not exist on crates.io)",
  "HRTF gated by feature = 'audio-hrtf' using the real 'hrtf' crate and 'oddio' spatial scene",
  "HRTF default off; HRIR sphere loaded at runtime via HrtfMode::from_file",
  "test: distance attenuation matches expected (mocked)",
  "test: HrtfMode defaults to Disabled, init matches feature flag, missing HRIR file returns Disabled, oddio scene creates"

[[items]]
id = "R-074"
title = "hyge-input crate skeleton (Action<T>, TOML, raw input)"
phase = "phase_7"
owner = "input"
priority = "P0"
status = "not_started"
risk = "low"
dependencies = ["R-012", "R-061"]
summary = "Create hyge-input with Action<T>, TOML bindings, hot-reload, raw input."
acceptance = [
  "crates/hyge-input/Cargo.toml declares deps on toml, serde, notify, hyge-ecs, hyge-window, hyge-core",
  "ActionMap with actions + bindings",
  "Action::Button, Axis, Vec2",
  "Binding enum (Keyboard, MouseButton, MouseAxis, MouseDelta, GamepadButton, GamepadAxis)",
  "TOML parsing matches schema in docs/architecture.md §6.10",
  "translate system reads DeviceEvents, updates ActionMap",
  "hot-reload of the .toml file rebinds without restart",
  "test: edge detection, scale aggregation"
]

[[items]]
id = "R-075"
title = "Raw input (Windows) and gilrs (gamepad)"
phase = "phase_7"
owner = "input"
priority = "P0"
status = "not_started"
risk = "medium"
dependencies = ["R-074"]
summary = "Raw input via RegisterRawInputDevices on Windows, gilrs for gamepad on all platforms."
acceptance = [
  "Windows: RegisterRawInputDevices integrated into winit event loop",
  "Linux/macOS: evdev direct (or winit fallback) behind same RawInput trait",
  "gilrs 0.10+ for gamepad (GamepadConnected/Disconnected events, axes, buttons)",
  "test: bind WASD to a vec2 action, assert values update when keys pressed (mocked)"
]

[[items]]
id = "R-076"
title = "hyge-script crate skeleton (mlua, sandbox, bevy_reflect)"
phase = "phase_7"
owner = "script"
priority = "P0"
status = "not_started"
risk = "medium"
dependencies = ["R-061"]
summary = "Create hyge-script with mlua engine, sandbox, bevy_reflect bindings, hot-reload."
acceptance = [
  "crates/hyge-script/Cargo.toml declares deps on mlua, bevy_reflect, hyge-ecs, hyge-core",
  "ScriptEngine wraps mlua::Lua",
  "Sandbox strips os, io, debug, package, require (raw)",
  "hyge.* API: log, world.entity.new/destroy, world.get/set (via Reflect), event.send, input.action, audio.play, time.delta",
  "ScriptRef component",
  "events::ScriptError",
  "test: forbidden stdlib actually stripped; component get/set round-trip"
]

[[items]]
id = "R-077"
title = "Script hot-reload and on_reload hook"
phase = "phase_7"
owner = "script"
priority = "P0"
status = "not_started"
risk = "medium"
dependencies = ["R-076"]
summary = "Notify-based hot-reload of Lua scripts, calling on_reload on the entity."
acceptance = [
  "notify watches assets/scripts/",
  "on change: re-parse; if OK, replace ScriptState, call on_reload(old_state)",
  "if parse fails, push ScriptError event, keep old script running",
  "test: modify a .lua file, assert ScriptState replaces and on_reload is called (mocked)"
]

[[items]]
id = "R-078"
title = "M5 smoke test: full systems scene with 5 prefabs"
phase = "phase_7"
owner = "app"
priority = "P0"
status = "not_started"
risk = "low"
dependencies = ["R-071", "R-072", "R-074", "R-076"]
summary = "M5 Definition of Done: scene with 5 prefab instances, 100 physics bodies, audio, input, Lua all hot-reloadable."
acceptance = [
  ".hyge-world with 5 prefab instances of a complex asset",
  "100 dynamic rapier3d rigid bodies, 1 character controller, collision events flow into ECS",
  "16 spatial audio emitters, music stream, bus mixer",
  "WASD + mouse look + gamepad mapped via TOML",
  "Lua: assets/scripts/player.lua controlling a cube via input + rigid body",
  "hot-reload of scripts, scene, input bindings, asset changes",
  "AGENTS.md §5.2 M5 DoD checklist satisfied"
]

# =============================================================================
# PHASE 8 — Full Visual Editor
# =============================================================================

[[items]]
id = "R-080"
title = "hyge-editor crate skeleton (egui + docking)"
phase = "phase_8"
owner = "editor"
priority = "P0"
status = "not_started"
risk = "medium"
dependencies = ["R-013"]
summary = "Create hyge-editor with egui + egui_dock + egui_tiles, EditorPlugin, default layout."
acceptance = [
  "crates/hyge-editor/Cargo.toml declares deps on egui, egui_dock, egui_tiles, hyge-app, hyge-ecs, hyge-render, hyge-scene, hyge-core",
  "EditorPlugin implements HygePlugin",
  "EditorUi resource with panel_state, selection, clipboard, history",
  "default layout per docs/architecture.md §12.1",
  "test: editor plugin loads in headless mode without panicking"
]

[[items]]
id = "R-081"
title = "Viewport panel"
phase = "phase_8"
owner = "editor"
priority = "P0"
status = "not_started"
risk = "high"
dependencies = ["R-080", "R-043"]
summary = "Editor viewport running the same render graph with editor camera + gizmos."
acceptance = [
  "owns a wgpu::Surface (or sub-region of main surface)",
  "runs the same RenderGraph as runtime with EditorCamera",
  "selection outline (stencil + outline pass)",
  "gizmos: translate, rotate, scale (per-entity handles)",
  "test: viewport renders a scene, selection outline visible on selected entity (snapshot)"
]

[[items]]
id = "R-082"
title = "Hierarchy panel"
phase = "phase_8"
owner = "editor"
priority = "P0"
status = "not_started"
risk = "low"
dependencies = ["R-080", "R-061"]
summary = "Tree view of World, drag-drop to reparent."
acceptance = [
  "tree from Parent/Children components, Name for labels",
  "drag-drop to reparent via Command::Reparent",
  "right-click context menu (add component, remove, duplicate, delete)",
  "test: reparent entity, assert hierarchy updated, undo restores"
]

[[items]]
id = "R-083"
title = "Inspector panel (reflect-driven)"
phase = "phase_8"
owner = "editor"
priority = "P0"
status = "not_started"
risk = "high"
dependencies = ["R-080", "R-061"]
summary = "Reflect-driven component editor; auto-generates fields from TypeRegistry."
acceptance = [
  "reads TypeRegistry, for each component of selected entity generates field editor from Reflect::ReflectRef",
  "f32/i32/u32/bool -> slider/drag/check",
  "Vec3/Quat/Mat4 -> multi-slider with gizmo",
  "String -> text edit",
  "Handle<A> -> drag-drop thumbnail + open-in-browser button",
  "nested Reflect -> collapsible group",
  "modifications emit Command::EditComponent for undo/redo",
  "test: modify a component value via inspector, undo, assert value restored"
]

[[items]]
id = "R-084"
title = "Content browser panel"
phase = "phase_8"
owner = "editor"
priority = "P0"
status = "not_started"
risk = "medium"
dependencies = ["R-080", "R-031"]
summary = "File tree of assets/, thumbnails, double-click to open."
acceptance = [
  "file tree of assets/",
  "thumbnails via PreviewRenderJob (headless renderer captures 256x256)",
  "double-click: select; on .hyge-world open scene; on .hyge-prefab instantiate; on .hyge-mesh preview",
  "test: open the content browser, assert file tree populated"
]

[[items]]
id = "R-085"
title = "Asset graph panel"
phase = "phase_8"
owner = "editor"
priority = "P1"
status = "not_started"
risk = "low"
dependencies = ["R-084", "R-031"]
summary = "Visualization of the BLAKE3 dependency graph."
acceptance = [
  "egui::Graph view of the BLAKE3 dependency DAG",
  "pan/zoom, click to inspect",
  "highlights cascade when an asset is selected",
  "test: render the asset graph for a known scene, assert nodes match expected count"
]

[[items]]
id = "R-086"
title = "Console + Profiler panels"
phase = "phase_8"
owner = "editor"
priority = "P0"
status = "not_started"
risk = "low"
dependencies = ["R-080", "R-025"]
summary = "Console (tracing ring buffer) + Profiler (frame stats sparkline)."
acceptance = [
  "Console: ring buffer of last 1000 log lines, filter by level/target, click to navigate",
  "Profiler: live sparkline of frame time, GPU time, draw calls, instance count, memory (egui::plot::Plot)",
  "test: emit a tracing event, assert it appears in console buffer"
]

[[items]]
id = "R-087"
title = "Selection and undo/redo"
phase = "phase_8"
owner = "editor"
priority = "P0"
status = "not_started"
risk = "low"
dependencies = ["R-080"]
summary = "Selection resource, command stack, Ctrl+Z / Ctrl+Y bindings."
acceptance = [
  "Selection resource: Vec<Entity>, multi-select with shift",
  "CommandHistory with undo/redo Vec<Box<dyn Command>>",
  "Commands: EditComponent, Reparent, Instantiate, Destroy, Duplicate, AddComponent, RemoveComponent",
  "Ctrl+Z pops undo, applies Command::revert, pushes to redo",
  "Ctrl+Y opposite",
  "test: 10 commands, undo 5, redo 3, assert state correct at each step"
]

[[items]]
id = "R-088"
title = "Editor camera gizmos + theme + docking persistence"
phase = "phase_8"
owner = "editor"
priority = "P1"
status = "not_started"
risk = "low"
dependencies = ["R-081"]
summary = "Camera gizmos (translate/rotate/scale handles), dark theme, docking layout save/load."
acceptance = [
  "gizmos: translate (3-axis arrows), rotate (3 circles), scale (3 cubes)",
  "dark theme default, light toggle",
  "docking layout serialized to user_data/editor_layout.json",
  "on editor start, layout restored; on close, layout saved",
  "test: serialize + deserialize dock tree, assert equality"
]

[[items]]
id = "R-089"
title = "M6 smoke test: editor + hot-reload of scene"
phase = "phase_8"
owner = "editor"
priority = "P0"
status = "not_started"
risk = "low"
dependencies = ["R-088"]
summary = "M6 Definition of Done: editor renders scene, hierarchy/inspector edit live, scene hot-reload preserves PersistOnReload."
acceptance = [
  "viewport rendering runtime scene with editor camera",
  "hierarchy + inspector editing components live (via Reflect)",
  "content browser shows assets/ with thumbnails",
  "hot-reload of .hyge-world preserves PersistOnReload entities",
  "undo/redo across all editor actions",
  "profiler panel pulling frame stats from runtime",
  "docking layout persists across sessions",
  "AGENTS.md §5.2 M6 DoD checklist satisfied"
]

# =============================================================================
# PHASE 9 — Polish Sandbox mdbook Performance Release
# =============================================================================

[[items]]
id = "R-090"
title = "hyge-runtime-test crate"
phase = "phase_9"
owner = "tooling"
priority = "P0"
status = "not_started"
risk = "low"
dependencies = ["R-023"]
summary = "Create hyge-runtime-test with headless wgpu harness, capture, compare, hash utilities."
acceptance = [
  "crates/hyge-runtime-test/Cargo.toml declares deps on wgpu, image, hyge-render, hyge-core",
  "TestRenderer struct (no surface)",
  "ReferenceImage { width, height, bytes }",
  "create_test_renderer(), capture_frame(), assert_image_matches(actual, expected_path, tolerance), hash_image()",
  "test: capture a frame from a known scene, assert hash stable"
]

[[items]]
id = "R-091"
title = "Headless screenshot tool (hyge-tools headless)"
phase = "phase_9"
owner = "tools"
priority = "P0"
status = "not_started"
risk = "medium"
dependencies = ["R-090", "R-063"]
summary = "CLI subcommand that boots App, loads a scene, captures a frame to PNG."
acceptance = [
  "hyge-tools headless --scene <path> --width W --height H --out <path>",
  "boots App with hidden (offscreen) wgpu surface",
  "loads .hyge-world, runs N frames, captures the Nth to PNG",
  "test: render a known scene, assert the output PNG is byte-stable"
]

[[items]]
id = "R-092"
title = "Snapshot tests for renderer output"
phase = "phase_9"
owner = "render"
priority = "P0"
status = "not_started"
risk = "low"
dependencies = ["R-090", "R-058"]
summary = "Reference scenes with golden PNGs, snapshot diff in CI."
acceptance = [
  "assets/tests/reference/ contains golden scenes (DamagedHelmet, SciFiHelmet, CubeField)",
  "tests/snapshots/ contains golden PNGs",
  "CI runs assert_image_matches on every PR",
  "tolerance configurable per test, default SSIM 0.99",
  "test: verify all goldens still match (post-cleanup test)"
]

[[items]]
id = "R-093"
title = "examples/hyge-sandbox full demo"
phase = "phase_9"
owner = "examples"
priority = "P0"
status = "not_started"
risk = "low"
dependencies = ["R-088"]
summary = "Full demo: PBR scene with IBL, shadows, post, animation, physics, Lua AI, audio, editor-driven."
acceptance = [
  "examples/hyge-sandbox/ is a binary crate",
  "loads a .hyge-world with a complex scene",
  "demonstrates every system from phases 1-8",
  "runs without warnings on cargo build --release",
  "smoke test: cargo run --example hyge-sandbox -p hyge-sandbox --release boots and renders"
]

[[items]]
id = "R-094"
title = "mdbook documentation site"
phase = "phase_9"
owner = "docs"
priority = "P0"
status = "not_started"
risk = "low"
dependencies = []
summary = "mdbook site with 4 tutorials published to GitHub Pages."
acceptance = [
  "docs/mdbook/ contains book.toml and src/ with SUMMARY.md",
  "Tutorial 1: Setup (install Rust, build, run hyge-sandbox)",
  "Tutorial 2: First scene (load glTF, place in .hyge-world)",
  "Tutorial 3: Scripting (Lua binding to ECS, hot-reload)",
  "Tutorial 4: Editor (open scene, edit component, hot-reload)",
  "GitHub Actions deploys to gh-pages on tag v0.1.0",
  "test: mdbook build succeeds with no broken links"
]

[[items]]
id = "R-095"
title = "Performance baseline + criterion benchmarks"
phase = "phase_9"
owner = "tooling"
priority = "P0"
status = "not_started"
risk = "low"
dependencies = ["R-090", "R-058"]
summary = "Capture performance baseline and integrate criterion benchmarks."
acceptance = [
  "docs/perf/baseline.json captures frame time, GPU time, draw calls for hyge-sandbox on RTX 3060",
  "cargo bench --workspace runs criterion for meshlet bake, BLAKE3 hashing, light grid build, GPU pass timing",
  "CI alerts on >10% regression in any benchmark",
  "test: criterion runs to completion, baseline.json validates against the schema"
]

[[items]]
id = "R-096"
title = "v0.1.0 release tag and GitHub release"
phase = "phase_9"
owner = "release"
priority = "P0"
status = "not_started"
risk = "low"
dependencies = ["R-095"]
summary = "Tagged release on GitHub with auto-generated notes."
acceptance = [
  "CHANGELOG.md generated by git-cliff",
  "git tag v0.1.0 created and pushed",
  "GitHub release published with auto-generated notes",
  "binary artifacts (hyge-tools, hyge-sandbox) attached to release",
  "all acceptance bullets in this phase's DoD met"
]

[[items]]
id = "R-097"
title = "M7 final DoD: 144 FPS at 1080p on RTX 3060"
phase = "phase_9"
owner = "release"
priority = "P0"
status = "not_started"
risk = "medium"
dependencies = ["R-096"]
summary = "M7 Definition of Done: final performance + quality gate before v0.1.0."
acceptance = [
  "performance: 144 FPS at 1080p on RTX 3060 with hyge-sandbox scene (10k static instances + 64 dynamic lights)",
  "cargo +stable build, no nightly",
  "no unsafe outside hyge-render/hyge-render-graph, each with // SAFETY: comment",
  "AGENTS.md §5.3 v0.1.0 DoD checklist 100% satisfied"
]
